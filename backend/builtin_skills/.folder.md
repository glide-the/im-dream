# backend/builtin_skills — Folder Contract

## Architecture Summary

Server-owned Agent Skills included by the backend-only Docker build and refreshed into every full thread workspace. Built-in names are authoritative; user/runtime-installed Skills with other names remain workspace-owned.

## File Table

| Path | Pos | Function |
|---|---|---|
| `notion-session/` | built-in-skill | Read-only Notion index navigation and virtual page-read guidance; it uses only `Read` and never exposes CLI, MCP tools, credential paths, or tokens. |

## Sync

- 2026-08-28: synchronize the useful upstream index/page/database guidance into the backend-owned package, adapting page content to `.notion/pages/<id>.json` lazy Read and removing Agent CLI/MCP ownership.
- 2026-08-28: establish a backend build-context source for `notion-session`, replacing the repository-root-only packaging path that disappeared in containers.
