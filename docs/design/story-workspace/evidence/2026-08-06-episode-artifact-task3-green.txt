Task 3 TDD Green and regression evidence
Captured: 2026-08-06 Asia/Shanghai

Backend focused Green:
  Command: PYTHONPATH=. .venv/bin/pytest -q [8 Red-target tests]
  Result: 8 passed in 0.52s

Backend related regression:
  Command:
    PYTHONPATH=. .venv/bin/pytest -q \
      tests/test_story_workspace_episode_artifact_adapter.py \
      tests/test_story_workspace_episode_auxiliary_artifact_adapter.py \
      tests/test_story_workspace_episode_artifacts_api.py
  Result: 339 passed in 4.55s

Frontend focused Green:
  Command:
    npx playwright test \
      src/hooks/story-workspace/__tests__/useStoryWorkspaceEpisodeArtifacts.test.ts \
      --reporter=line --workers=1 \
      -g "unavailable root preserves|strict parser rejects source-key"
  Result: 2 passed (501ms)

Frontend related regression:
  Command:
    npx playwright test \
      src/hooks/story-workspace/__tests__/useStoryWorkspaceEpisodeArtifacts.test.ts \
      src/pages/story-workspace/__tests__/StoryWorkspaceExecutionEpisodeIntegration.test.ts \
      src/components/story-workspace/episode/__tests__/StoryWorkspaceEpisodeNarrativeWorkbench.test.tsx \
      src/components/story-workspace/episode/__tests__/StoryWorkspaceEpisodeReviewPanel.test.tsx \
      src/components/story-workspace/episode/__tests__/StoryWorkspaceEpisodeShotAuxiliary.test.tsx \
      --reporter=line --workers=1
  Result: 72 passed (5.3s)

Static verification:
  npx tsc -b: PASS
  ESLint over every changed frontend source/test file: PASS
  git diff --check: PASS

Covered Green facts:
  - S04-E01-020a and S04-E01-020b retain exact narrative UUID seeds.
  - Review S04-E01-020A resolves to canonical public sourceKey S04-E01-020a and its existing narrative view ID.
  - A-key/B-ID cross-wire is rejected even when B is a real view ID.
  - Prompt, Render Queue and Review case-variant collisions fail closed.
  - Unknown targets remain orphan/null; no positional pairing is introduced.
  - Verified os.read/os.listdir EIO is per-root unavailable; symlink/path/identity tests remain global failures.
  - unavailable keeps only the affected per-root last-good and recovers on a later available revision.
  - No ChatView, browser-owned artifact truth, sensitive path, raw tool value, credential or hidden reasoning enters the surface.
