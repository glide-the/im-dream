U7 focused Node seam (from frontend/):
npx playwright test src/hooks/story-workspace/__tests__/StoryWorkspaceEpisodeActionV2.test.ts --reporter=line --workers=1

Result: 2 passed (249ms).

U7 Execution confirmation integration:
npx playwright test src/pages/story-workspace/__tests__/StoryWorkspaceExecutionEpisodeIntegration.test.ts --reporter=line --workers=1

Result: 13 passed (3.4s).

Existing Episode artifact/Continue compatibility regression:
npx playwright test src/hooks/story-workspace/__tests__/useStoryWorkspaceEpisodeArtifacts.test.ts --reporter=line --workers=1

Result: 35 passed (1.4s).

The tests prove:
- strict OptionV2 parsing, including all 183 classified public string fields;
- action-specific canonical inputs/revisions and target Episode in confirmation;
- V2 POST body contains actionId/idempotencyKey/userGuidance only;
- preview actions fail before fetch;
- legacy surfaces remain read compatible during the server migration;
- the page does not mount ChatView for confirmation.

TypeScript:
npx tsc -b
Result: exit 0.

Focused ESLint over all U7 frontend files:
Result: exit 0.
