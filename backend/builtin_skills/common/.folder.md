# backend/builtin_skills/common — Folder Contract

## Architecture Summary

Platform-independent builtin Skill namespace. Every full thread workspace
selects these packages; directory packages are exposed through exact read-only
source symlinks and keep their canonical flat Skill IDs at Runtime.

## File Table

| Path | Pos | Function |
|---|---|---|
| `skill-creator/` | common built-in Skill | Complete Skill authoring/evaluation package, always available when Workspace Mode is enabled. |

## Sync

- 2026-09-01: move `skill-creator` from the root-flat source tree into the
  always-selected common namespace without changing its Runtime ID.
