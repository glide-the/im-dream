# deploy/docker/

## 架构概述
Docker 发布脚本目录，复用根目录 `docker-compose.yml`、前后端 Dockerfile 和 nginx 模板。

## 文件清单

| File | Pos | Function |
|------|-----|----------|
| deploy.sh | Docker 发布入口 | 检查 Docker/Compose 与本地配置，构建、启动、验证、停止和清理 Compose 服务 |
