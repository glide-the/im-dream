# docs/design — 目录合同

## 架构摘要

Ink & Memory 的模块级实现设计文档，记录功能边界、交互流程、API、扩展点和验收合同。

## 文件清单

| 文件 | 定位 | 职责 |
|------|-----|----------|
| `claude-agent.md` | 设计文档 | Claude Agent 功能边界、时序、API、服务职责、隔离和扩展点 |
| `deck-claude-agent.md` | 设计文档 | Deck voice 与 Claude Agent 的 Thread、Memory、widget 和 Chat 集成 |
| `sequence-diagrams.md` | 时序文档 | 认证、Session、分析、Deck、好友和语音输入等当前业务时序 |
| `writing-manual-suggestions.md` | Writing 交互与实现设计 | 定义手动触发、持久 Suggestion Cell、Session 级 Claude Thread、SSE 增量、仅最新建议可重新生成以及重试与恢复合同 |
| `claude-agent/` | Claude Agent 设计集 | 自有 SDK、clean-room Runtime、上下文、Workspace/TMPDIR、SSE/EventBus、MCP Apps/Agent UI 集成、兼容和内存报告 |
| `claude-mcp/` | Claude MCP 设计集 | Resources、clean-room Runtime 身份、OAuth/refresh、Tools inventory、sandbox credential 和回滚 |
| `dream-agent/` | design-doc-set | Dream/Chat 单一 thread runtime、当前业务交互时序、DreamObserver、Hook 自动同步、旧协议迁移删除、设计审查与验收入口 |
| `story-workspace/` | design-doc-set | Story Workspace 当前产品边界、Chat-first 与 Dream 重入；Deck 主设计统一链接到 `deck/` |
| `deck/` | deck-design-doc-set | PDF 驱动的 Deck 首页、创建弹窗、有效评估器式版本设计、Chat/Dream 消费边界、时序与追踪矩阵 |
| `deck-plugin/` | deck-plugin-interaction-doc-set | ClaudePlugin 与 Deck-adjacent 插件管理交互；包含 Admin-owned 全局远程 Marketplace 与 capability-gated 安装闭环 |
| `deck-register/` | deferred-deck-distribution | 暂缓的 Deck 市场注册、发布、安装与分发治理需求 |
| `edit-session/` | design-doc-set | 编辑器会话模块设计文档集：核心对象、功能清单、生命周期、事件流、对象泳道图、扩展点与限制 |
| `file-storage/` | design-doc-set | 文件存储模块设计文档；客户端上传示例使用根部署 API 路径 `/api/...` |
| `memory/` | design-doc-set | Claude Agent procedural Memory Workspace 设计：分区配置表来源、文件接口初始化边界、迁移说明 |
| `remote-ssh-interaction-plan.md` | design-doc | Remote SSH 一键部署交互设计：主机 nginx、远端存储、Compose 部署与数据维护命令边界 |
| `diary-import-agent-labels.md` | design-doc | 日记导入脚本 --label-mode agent 设计：DB 预检跳过已存在 session、tqdm 进度条、--workers 多线程并发、Token 自动生成、SSE 解析 |
| `notion-session/` | design-doc-set | Notion Device 资源连接器设计文档集：overview 四层抽象、connector-interaction 交互方案 |

## 同步记录

- 2026-09-12：Claude Agent 工作区 ZIP 设计改以 Dream PreToolUse 为唯一策略 owner，允许显式普通输入并拒绝 dot runtime、越界、glob、符号链接和 shell 组合。
- 2026-09-06：Dream Web 设计同步 `54f3bbe5` 的 Next.js 16 + pnpm workspace 当前实现：唯一 App Router 为 `frontend/app/**`，唯一 Dream 应用源码为 `frontend/app/_dream/**`，`frontend/packages/mcp-apps-runtime/src/**` 是合法 server-only 独立包；Python 仍拥有身份和业务数据，Next Route Handler 仅作同源薄适配；MCP Apps 仅完成 provider-free 技术 preview，尚无公开应用与生产启用，`productionAppsEffective=false`。
- 2026-09-06：MCP Apps 执行清单保留 Phase 0→3、DEC-002/004/005、安全边界与 50 项验收，并统一使用当前证据判断完成度。
- 2026-09-05：SUO-404 / `DEC-005` 将 `frontend/` 固定为 Dream Web 的 pnpm workspace/package/Next 根，App Router 只能是 `frontend/app/**`，Node MCP Runtime 只能是同级 `frontend/packages/mcp-apps-runtime/**`；旧嵌套项目、legacy server 路径及其旧证据失效，旧 npm lock 下的 P0 Go 也须重跑。
- 2026-09-05：SUO-403 / `DEC-004` 废止 Phase 1 自建 AppServer，S1-01 改为原样复用 tag/commit/npm SRI/provenance 锁定的官方 `basic-server-vanillajs@1.7.5`；Host/Node/Python 边界与 production Apps 关闭状态不变，下游须重做当前候选验证。
- 2026-09-05：SUO-397 原位纠正 MCP Apps S1-01/DEC-003 为仓库自建、IM-owned、provider-free AppServer；该路线已由同日 SUO-403 / `DEC-004` 明确取代。
- 2026-09-04：SUO-384 最终接受 MCP Apps `DEC-002`，不建立平行 renderer 方案；补齐 production Gate、outer iframe 导航前安全提交点与回到同一次普通工具结果的回滚路径，继续仅交回 P0-04/P0-08 有界重跑。
- 2026-09-04：SUO-386 验收 MCP Apps `DEC-002`，在主设计中锁定 P0-04/P0-08 重跑的四项精确依赖/Browser 版本、唯一 Playwright 入口、命令和 lock 失配回退；方案、安全边界及 production 关闭状态不变。
- 2026-09-04：SUO-383 根据 task_301 P0-04 的 Chrome/bundle 证据修正 MCP Apps Browser Host：锁定 `@mcp-ui/client@7.1.1` 仅复用 AppBridge/transport，最小 Host adapter 持有完整 resource metadata 与 permissions→两层 iframe allow；P0-04/P0-08 有界重跑前保持 No-Go 和 production Apps 关闭。
- 2026-09-04：SUO-374 验收 `cfd75b38` 初始结构基线：MCP Apps 集成收束为六份设计稿和一份系统架构执行清单，Session Start、Next.js、Browser→Node 受控 MCP、`PersistentConnectorManager` 与 Python 配置职责已固定，50 个执行项可供下游按大模块消费；其中初始 AppRenderer iframe ownership 已由同日 SUO-383 / DEC-002 增量取代。
- 2026-09-01：Writing 建议改为仅手动触发的持久 Suggestion Cell；一个 Writing Session 懒绑定一个产品级 Claude Agent Thread，移除 2 秒自动 debounce、随机 Voice/Deck 身份和临时 InspirationHint，复用现有 Session JSON 与 Claude SSE。
- 2026-09-01：Writing 建议序列只允许最后一个 Suggestion Cell 展示 Refresh/Retry；历史建议继续持久显示但不可重新触发。
- 2026-08-31：删除每日图片生成、定时调度和旧 session runtime 时序；Timeline 仅保留历史图片读取。
- 2026-08-31：普通 Writing 编辑停止 `analyze_text` 自动模型请求；写作灵感从 `get_writing_suggestion` PolyCLI session 迁移到已启用 Voice 的 Claude Agent SSE Thread，并逐段投影 `text-delta` 打字机文本，正文快照校验与 2 秒防抖保持不变。
- 2026-08-31：Story Workspace 新增 Dream Thread 绑定冲突与恢复设计，并补齐 dream-agent/story-workspace 目录 inventory；合法同 Deck Agent 切换与真正完整性冲突分离。
- 2026-08-28：Notion 上游差异评审以 `develop@e3523db9` 和固定 `ntn 0.15.1` 为证据，识别 data source ID 与旧 endpoint 错配；后台同步只建轻量索引，正文只由 Runtime selected-page Read hook 调用 Markdown endpoint。
- 2026-08-28：Notion 设计新增 agentdata 用户凭证源、thread 私有索引投影、只读 page hook、backend-owned Skill 装载、失败降级和三方案/反过度设计评审；不新增 schema、服务、Agent-visible MCP 或 Agent 状态机。
- 2026-08-28：Claude Agent SDK/Runtime 设计采用公开 Runtime `0.1.3`，记录 opaque 模型能力投影、Messages 请求参数合同、same-SHA workflow、registry fresh install 与 Secret 最小权限/保留边界。
- 2026-08-24：Claude Agent/MCP 设计更新为正式 PyPI SDK `0.2.143` 版本/哈希锁、MIT clean-room Runtime `main@7c34e6cd`/公开 npm 0.1.0、13 项 IM capability、Bun 1.4.0 四平台/五包、registry fresh install、零 `.map`、OAuth refresh rotation 修复、内存实测和最终 Comfy 两轮 Tool/resume 回执；本轮当前态正文统一为中文。

- 2026-08-22: restored the Claude MCP Resources design and implementation contract onto current `develop` while preserving thread-scoped temp isolation.
- 2026-08-22: Claude Agent resource diagnosis records the remote global OOM baseline and the optimized resume-turn read storm; the minimal reversible fix combines legacy MCP default-off, in-process concurrency/memory admission, and an Alibaba Compose block-I/O budget. Remote Control, swarm, strict MCP, bare mode, Node heap/CPU wrappers, and vendor patches are excluded by evidence or compatibility constraints.
- 2026-08-22: Claude Agent designs move `CLAUDE_CODE_TMPDIR` from shared `/tmp/claude` to `{AGENT_CWD}/{thread_id}/.claude-tmp`; Workspace Mode disabled creates only this runtime root while keeping `cwd`, context, file surfaces, and sandbox settings disabled.
- 2026-08-19: 远程 ClaudePlugin Marketplace 明确为所有 Dream 用户共享的全局目录；v1 不依赖 Marketplace 对象存储桶，同步 clone 仅为任务临时目录。
- 2026-08-19: `deck-plugin/claude-plugin-remote-marketplace.md` 定义 Admin-owned 远程来源、同步 revision、条目审批、Dream 安装 lineage 和 Comfy 隔离 CLI 验证；不使用 local source 常量作为生产目录。
- 2026-08-19: `deck-plugin/claude-plugin-marketplace-add.md` 与 `claude-plugin-remote-marketplace.md` 已对齐实现：Admin `0037`、全局目录、entry-ID 安装、ref/commit/manifest/完整插件 digest 复验与 capability 缺失 503。
- 2026-08-19: 新增 `deck-plugin/`，记录 ClaudePlugin 创建操作菜单、真实安装入口和 Marketplace catalog 缺失时的 fail-closed 交互。
- 2026-08-16: Deck 主设计全部迁入 `deck/`；PDF 首页保持有效，创建后恢复任务 `01a00576` 之前的 Deck/Agent/Prompt/插件引用维护弹窗，Workflow 仍排除，市场分发继续暂缓。
- 2026-06-14: `edit-session/session-event-driven-sync.md` 更新为最小事件驱动实现稿，替代 Agent MCP 写确认后的 2 秒盲等待策略。
- 2026-06-14: `edit-session/session-event-driven-sync.md` 补充自动保存内容签名去重，避免无内容变化时排队 3 秒 debounce 或请求 `/api/sessions`。
- 2026-06-15: `file-storage/README.md` client examples now use root deployment API paths after removing the `/ink-and-memory/` frontend prefix.
- 2026-06-16: `remote-ssh-interaction-plan.md` now limits data maintenance to `backup` / `upload` / `download`, with upload force-recreating Compose services.
- 2026-06-16: `claude-agent/session-labels-and-retrieval.md` documents configurable `get_sessions_range` fuzzy query retrieval, labels filtering, and vector interface-only boundary.
- 2026-06-16: workspace and claude-agent design docs document importing real `.claude/skills/` writes into `workspace/skills/` before rebuilding skill discovery symlinks.
- 2026-06-21: `claude-agent/claude-agent-sandbox-network-interaction-plan.md` documents Settings-backed sandbox network policy and updates workspace sandbox docs.
- 2026-06-28: `notion-session/` and `claude-agent/notion-point/` define the Notion resource connector MVP around connector-owned canonical snapshots, frontend interaction states, business flowcharts, and minimal snapshot contract code.
- 2026-07-05: `notion-session/connector-interaction.md` 增补认证会话保持语义（poll 幂等/消费态不回退）与 Notion `v1/search` 的 `database` 过滤映射修正。
- 2026-07-07: `notion-session/` docs now describe the Chat-first connector entry: app navigation owns the landing state, and the connector workbench lives under the composer instead of as a standalone primary page.
- 2026-07-07: `notion-session/` docs also pin the composer-adjacent quick-action strip and recoverable `shell_error` fallback so `ChatViewContent` render failures do not erase the connector entry path.
- 2026-07-07: `notion-session/connector-interaction.md` now separates embedded `聊天` fallback workbench state from the real `来源` no-connector empty state and records the viewport-contained scroll contract.
- 2026-07-08: `notion-session/` docs update the connector entry to Settings-managed resource links with a lightweight Chat landing panel and a reusable Notion page-mode manager.
- 2026-07-08: `notion-session/` docs now align selected-resource persistence on `connector_resources` / connector `sources`, require Settings and Chat to share the same persisted sources, and filter Notion People system data sources at discovery.
- 2026-07-26: `claude-agent.md` 与 `lifecycle/claude-agent-lifecycle.md` 的 SDK 引用随 `claude-code-sdk` → `claude-agent-sdk 0.2.128` 迁移改名（`claude_agent_sdk`）；claude-agent 子目录迁移细节见 `claude-agent/.folder.md` 同日条目。
- 2026-08-11: 新增 `dream-agent/` 设计集；Dream conversation 直接复用 Chat thread history/status/SSE/confirmation/Stop，旧 run-scoped Dream Event/SSE 设计不再是规范。
- 2026-08-13: Dream 当前设计收束为共享 Chat、随机 Skill、Slash 安装建议和成功边界 Hook 自动同步；旧 B01–B21 阶段目录与推荐状态机不再作为业务规范。
- 2026-08-13: `dream-agent/deck-output-sync-design.md` 收敛为最小工作台闭环：根主 Agent turn 成功后自动同步 `.dream` stage 与 Run preview，页面沿用 `dream-files`；不引入命令编排、MCP 前置、watcher、PostToolUse 或 Observer 同步 owner。
- 2026-08-13: Dream/Story Workspace current designs reserve trusted/authoritative terminology for identity, provenance, permissions, CAS, and tamper boundaries; artifact workflows use build, sync, validate, and artifact-association wording.
- 2026-08-13: `dream-agent/project-episode-artifact-contract.md` records confirmation-turn automatic EP01 publication/binding and the execution page's read-only pending/linked presentation without a manual build action.
- 2026-08-14: `dream-agent/asset-collaboration-design.md` 区分资产列表摘要与聚焦完整正文；两者由同一成功 Hook/stage revision 发布，不增加详情 API、GET 副作用或页面 canonical 直读。
- 2026-08-14: `deck/chat-first-deck-defaults.md` defines the minimal Chat-first entry, temporarily hidden legacy navigation, screenplay-role Deck defaults, fail-closed Drama Forge binding, non-destructive zero-ref repair for existing default accounts, font scope, business sequences, and design review.
- 2026-08-14: The same Deck design now defines actor-owned publication display plus server-enforced default-publish and self-collection restrictions.
- 2026-08-14: `deck/dream-chat-agent-deck-interaction.md` aligns Dream/Chat Agent Deck behavior with the existing Chat PRD: only the resource-connector peer tab is replaced by an adaptive horizontal Dream list, with initial/in-progress-only re-entry semantics.
- 2026-08-14: Dream home visual guidance removes dashboard borders/stat blocks, uses large whitespace, and caps the active summary at three rows behind an accessible “查看更多” reveal.
- 2026-08-14: Dream/Story Workspace 当前设计删除 Execution“工作空间更新流”及其 revision 时间线；剧本创作台只消费当前人物、场景、分镜和 Episode 产物，底层 Hook/stage revision 合同保持不变。
- 2026-08-15: `deck/deck-versioned-chat-workspace.md` defines immutable Deck aggregate revisions, explicit historical-thread workspace upgrades, the quiet top-context UI, and the Admin Drizzle capability required before implementation.
- 2026-08-15: `deck/deck-evaluator-interaction-draft.md` is an active PDF-led contract; it borrows only Deck aggregate-version identity, diff, submission, and conflict concepts from CozeLoop.
