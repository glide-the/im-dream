
## 页面结构草图

```txt
┌────────────────────────────────────────────────────────────────────┐
│ ←  资源连接器  >  Notion Connector                                  │
└────────────────────────────────────────────────────────────────────┘


┌────────────────────────────────────────────────────────────────────┐
│                                                                    │
│   [连接器图标]  Notion Resource Connector                           │
│              Notion 远程资源连接器说明                              │
│              ● 已连接 / 未认证 / 已禁用                              │
│                                                                    │
│                                            [查看设计稿] [关闭连接]    │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘


┌────────────────────────────────────────────────────────────────────┐
│ 连接器概览                                                         │
│                                                                    │
│ ┌────────────────────────────────────────────────────────────────┐ │
│ │ 图标              Notion Connector                             │ │
│ │ 描述              连接 Notion 数据库、页面，用于对话与资源检索    │ │
│ │ 状态              未认证 / 已连接 / 同步中 / 错误                │ │
│ │ 设计稿            [查看链接器设计稿]                             │ │
│ │ 连接控制          [关闭连接]                                    │ │
│ └────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────┘


┌────────────────────────────────────────────────────────────────────┐
│ 资源范围                               [搜索资源] [保存资源] [刷新同步] │
│                                                                    │
│ 共 122 个资源，已选择 1 个                         第 1 / 13 页，每页 10 个 │
│ ┌────────────────────────────────────────────────────────────────┐ │
│ │ ☑ Data source  Openclaw-Obsidian-杭州2026-6-12        0 pages  │ │
│ │ 说明：Notion data source，可作为对话上下文来源                   │ │
│ ├────────────────────────────────────────────────────────────────┤ │
│ │ ☐ Data source  People                                  0 pages  │ │
│ │ 说明：Notion data source，可作为对话上下文来源                   │ │
│ ├────────────────────────────────────────────────────────────────┤ │
│ │ ☐ Page         产品设计文档                                      │ │
│ │ 说明：Standalone page，可作为对话上下文来源                      │ │
│ └────────────────────────────────────────────────────────────────┘ │
│                                                     [上一页] [下一页] │
│                                                                    │
│ ┌ - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - ┐ │
│ │                    当前未认证，资源选择不可用                  │ │
│ │                    请先完成 Notion 授权                         │ │
│ └ - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - ┘ │
└────────────────────────────────────────────────────────────────────┘


┌────────────────────────────────────────────────────────────────────┐
│ 已挂载来源                                                         │
│                                                                    │
│ ┌────────────────────────────────────────────────────────────────┐ │
│ │ Openclaw-Obsidian-杭州2026-6-12                    已同步       │ │
│ │ Database · Notion database                         0 pages      │ │
│ │ 最近同步：7月8日 21:12                                          │ │
│ └────────────────────────────────────────────────────────────────┘ │
│                                                                    │
│ 空态：还没有挂载来源；认证并保存资源选择后，来源必须立即出现在这里。 │
└────────────────────────────────────────────────────────────────────┘


```

---

## 页面信息

### 1. 顶部导航

图片里最上方是轻量导航，不是重型侧边栏。

结构应为：

```txt
返回按钮 / 面包屑 / 当前连接器名称
```

用于告诉用户：
当前不是一个独立设置页，而是从“资源连接器”进入的某个连接器详情页。

---

### 2. 连接器头部区域

图片顶部中间有头像、标题、状态标签，右侧有两个操作按钮。

对应到你的业务：

```txt
连接器图标
连接器名称
连接器描述
连接状态
查看链接器设计稿
关闭连接
```

状态建议分成：

```txt
未认证
认证中
已连接
同步中
同步失败
已关闭
```

---

### 3. 主配置卡片

图片中间最大的卡片是多行配置表单，每一行左侧是说明，右侧是下拉选择。

适合承载：

```txt
连接器概览
资源选择
同步范围
权限范围
默认使用方式
```

不过你说“策略暂时不实现”，所以不要把策略做成完整表单。
否则页面会显得功能很多，但实际不可用。

---

### 4. 资源范围与已挂载来源

图片中部的资源区域应承载“选择可用资源”和“展示已挂载来源”两个明确任务。

资源范围标题使用：

```txt
资源范围
```

未认证时：

```txt
禁用状态
展示空态说明
提示先完成 Notion 认证
```

认证后：

```txt
展示统一资源列表
资源行用 Data source / Page 标签区分
搜索框与「保存资源」同在操作行
默认每页 10 条
通过上一页 / 下一页翻页
点击资源行切换选中状态
点击保存资源后，已挂载来源立即显示所选资源
```

交互草图：

```txt
资源范围 [搜索资源] [保存资源] [刷新同步]
  ├─ ☑ Data source · 产品资料库 · 18 pages
  ├─ ☐ Data source · 调研记录 · 12 pages
  ├─ ☐ Page · 品牌规范
  └─ [上一页] 第 1 / N 页 [下一页]

已挂载来源
  └─ 产品资料库 · Database · 已同步
```

---

### 5. 授权 / 同步解释位置

底部不再保留独立的“授权 / 同步状态”卡片，也不展示开关。

当前连接器为何受限，应统一由 `NotionAccountStatusSection` 解释：

```txt
授权状态：未认证 / 已认证 / 认证中 / 同步失败
挂载来源：当前已保存来源数量
最近同步：最近一次同步时间或未更新
```

---

## 推荐最终页面层级

```txt
ConnectorConfigPage
├── TopNavigation
│   ├── BackButton
│   └── Breadcrumb
│
├── ConnectorHeader
│   ├── ConnectorIcon
│   ├── ConnectorName
│   ├── ConnectorDescription
│   ├── ConnectorStatusBadge
│   ├── ViewDesignButton
│   └── DisconnectButton
│
├── NotionAccountStatusSection
│   ├── AccountModelMetric
│   ├── AuthStatusMetric
│   ├── MountedSourcesMetric
│   └── LastSyncedMetric
│
├── ResourceScopeSection
│   ├── SectionHeader
│   │   ├── Title: 资源范围
│   │   ├── SearchInput
│   │   ├── SaveButton
│   │   └── RefreshButton
│   ├── UnifiedResourceList
│   │   ├── DataSourceRow
│   │   └── PageRow
│   ├── Pagination
│   └── EmptyDisabledState
│
└── MountedSourcesSection
    ├── SourceCard
    └── EmptyMountedState
```

---

## 关键交互规则

### 未认证状态

```txt
连接器概览：可查看
资源选择：禁用
统一资源列表：不展示
已挂载来源：保留当前来源或显示空态
账号状态：提示完成 Notion 认证
```

### 已认证状态

```txt
连接器概览：展示连接状态
资源范围：展示统一 data_source / page 列表
搜索资源：按标题 / 说明过滤
翻页：每页 10 条
保存资源：写入当前选择
已挂载来源：保存后立即展示所选资源和同步状态
```

### 关闭连接

```txt
点击关闭连接
弹出二次确认
确认后：
  - 状态变为已关闭
  - 资源选择禁用
  - 已选资源保留但不可用
  - 对话页不再调用该 connector
```
