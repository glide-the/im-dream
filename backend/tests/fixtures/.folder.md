# backend/tests/fixtures — Folder Contract

## Architecture Summary

Explicit test-only subprocess fixtures. These programs model external protocols deterministically and may write only to test-owned temporary paths supplied by the test harness. Production code must never select them.

## File Table

| File | Pos | Function |
|---|---|---|
| `claude_mcp_fake_cli.py` | Claude MCP argv/PTTY fixture | Model version/list/get/login/logout, redirect stdin, timeout, malformed output, cancellation, and non-zero exit without real OAuth. |
| `claude_agent_sandbox_fake_provider.py` | Claude Agent Runtime fixture | Serve deterministic Anthropic SSE tool-call responses; CLI defaults prove ordinary workspace writes and credential denial, while the reusable handler factory accepts test-owned fixed commands for process-isolated Runtime contracts. |
| `notion_cli_native.c` | native Notion CLI fixture | Requires synthetic server-bound environment and readable thread config before emitting success; compiled outside workspace, without real credential/remote I/O, never selected in production. |
| `claude_registry_release.json` | registry release fixture identity | Supply non-production exact SDK/Runtime/CLI versions and the five-package inventory for local archive/runner tests; contains no registry URL, token, model data, or production artifact. |
| `claude_mcp_inventory_server.py` | stdio/SSE/Streamable HTTP inventory fixture | Expose deterministic read-only tool/resource/prompt inventory through standard FastMCP; optional two-page SDK `nextCursor` mode remains explicit and production never selects it implicitly. |

## Sync

- 2026-09-13: the reusable fake-provider factory accepts caller-owned tool
  name/input for MCP result transport; default Bash CLI behavior is unchanged.

- 2026-09-06: extended the provider-free inventory fixture with an explicit loopback Streamable HTTP lane for production discovery and redirect-policy tests.
- 2026-09-04: the existing Claude sandbox fake provider exposes a compatible
  handler factory so notion-cli Runtime coverage can reuse the same Anthropic
  SSE protocol fixture without external provider, OAuth, credential, or content
  access; command and final text remain caller-owned test fixtures.
- 2026-08-24: Added provider-free Claude registry release identities used only with local synthetic wheel/sdist/npm archives.
- 2026-08-19: Added deterministic Claude MCP fake CLI and container sandbox provider fixtures; neither contacts a real OAuth provider or model.
- 2026-08-25: Added a standard FastMCP stdio/legacy-SSE inventory fixture selected only by explicit test profile or harness endpoint.
- 2026-08-25: Added an opt-in two-page standard-MCP cursor mode for direct Dream pagination integration coverage.
