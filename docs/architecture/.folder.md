# docs/architecture — 目录合同

## 架构摘要

Ink & Memory 的项目级和后端架构文档，覆盖系统拓扑、模块边界、API、认证、数据流、Claude Agent SDK/Runtime 与发布边界。

## 文件清单

| 文件 | 定位 | 职责 |
| --- | --- | --- |
| `claude-agent.md` | Claude Agent 架构 | 目录分层、API、生命周期、配置（四项 JSON-safe 正整数、组合内存与 no-restart LKG refresh）、数据流、错误处理、测试和隔离 |
| `项目架构设计说明.md` | 项目架构总览 | 当前 Vite/FastAPI/Admin/Gateway/PostgreSQL 拓扑、自有 SDK/Runtime 版本矩阵、调用链、数据生命周期、构建发布与回滚 |
| `auth.md` | 认证架构 | Google OAuth/OIDC、用户绑定、Token、安全边界和测试 |
| `auth-device.md` | Device Flow 架构 | OAuth 2.0 Device Authorization Grant 路由、状态、轮询、安全和时序 |
| `术语表.md` | 架构术语 | 项目通用术语与语义边界 |

## 同步记录

- 2026-08-28：项目总览采用公开 Runtime `0.1.3`，记录 opaque alias 的 `max_tokens=384000`/effort 请求修复、same-SHA 发布、provider-free registry 验收和默认 Dream 服务切换。
- 2026-08-28：`claude-agent.md` 将四项配置统一到正安全整数与精确组合内存边界，并记录 higher-revision 动态 apply、same-revision no-replace、invalid/unavailable LKG 与 Agent 核心语义不变。
- 2026-08-26：项目总览同步本机 Runtime 安装拓扑：默认软链接指向 registry `0.1.1` 内容寻址树，旧 `0.1.0` 树仅保留为回滚证据。
- 2026-08-25：项目总览同步 Dream-managed MCP：Server 列表只读 PostgreSQL，标准 MCP SDK 负责连接/auth discovery，移除 Runtime `mcp list/get` 作为管理事实来源的旧说明。
- 2026-08-26：项目总览同步正式 PyPI SDK `0.2.144`、npm Runtime `0.1.1`、same-SHA workflow 回执、registry tarball 哈希与 Dream 精确配对。
- 2026-08-25：`项目架构设计说明.md` 将 HTTP MCP 与 OAuth 解耦，记录匿名优先探测、正交认证状态、稳定失败码和 bare 403 不触发认证的 Runtime/Dream 边界；并同步匿名/OAuth Chat、Dream Workflow、Workspace/sandbox、Runtime 故障恢复与 Admin Gateway/账本真实验收。
- 2026-08-24：`项目架构设计说明.md` 链接中文 SDK/Runtime 打包、PyPI/npm 发布与 Dream 集成操作手册，确保架构版本矩阵和实际执行步骤可互相追踪。
- 2026-08-24（历史阶段）：`项目架构设计说明.md` 初次重写为 MIT clean-room Runtime：SDK 当时固定 `main@6164bd91…`，记录 13 项 IM 能力、五个 npm 平台包、无 `.map`、内存实测和真实 Comfy 两轮 tool/resume 回执；历史恢复源码明确排除出公共构建输入。
- 2026-08-24（历史阶段）：`项目架构设计说明.md` 增补 SDK/Runtime 统一 `main`、PyPI/npm 五包发布链、零 source map 合同；Runtime `main@7c34e6cd` 与 npm 五包 registry fresh install 已完成，SDK 当时仍为独立未发布边界。当前发布状态见下一条。
- 2026-08-24：SDK `0.2.143` 已通过 Trusted Publisher 发布到 TestPyPI/正式 PyPI；架构总览改为 Dream 精确 PyPI 版本与 wheel/sdist SHA-256 锁，记录 source tag、发布 run 和远端制品身份，npm 仅继续承载独立多平台 Runtime。
- 2026-08-24：`项目架构设计说明.md` 移除旧 SQLite/runtime DDL/official SDK 默认路径，更新为 Admin-owned PostgreSQL、自有 SDK 0.2.143、production-qualified Runtime 0.1.0、Bun 1.4.0、本机安装与官方绝对路径回滚合同。
- 2026-08-23：`claude-agent.md` 记录 active-turn/host/cgroup memory admission、legacy memory MCP default-off、Workspace 图片预览与 Chat 长图导出鉴权边界。
- 2026-07-26：`claude-agent.md` 的 SDK 引用随 `claude-code-sdk` → `claude-agent-sdk` 迁移更新为 `claude_agent_sdk`。
- 2026-06-23：增加 Google OAuth/OIDC 与 Device Flow 架构文档。
