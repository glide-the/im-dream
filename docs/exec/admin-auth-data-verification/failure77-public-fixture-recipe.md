<!-- [Input] Actual safe command receipt or preserved validation plan; pinned disclosure scan passed. -->
<!-- [Output] Executed bounded evidence including original failures and exact outcomes. -->
<!-- [Pos] Coordinator-owned technical evidence; full migration and real acceptance remain separate. -->
<!-- [Sync] 2026-09-17: replace retired static service secret fixtures with short-lived client_credentials access tokens. -->

# Failure77 private fixture recipe

Provider-free isolated component scope only. Producer reviewed registered77 and prior76 FULL/PFreceipt/delegation parity. Public verifier is tests/integration/adminDreamLaunchFailure.contract.ts in /Users/dmeck/.codex/worktrees/729f/ink-admin-memory. Primary exclusively prepares new named removable PostgreSQL, technical subjects/canonical entities/tokens, seeds, faults and cleanup. Never run against normal data/accounts or read/print private JSON or DSNs in a producer/runner output. Source adapter does not execute SQL, provider, filesystem or Runtime.

## Strict fixture fields

Only these top-level keys: database, port, data_directory, target_verification_url, issuer, service_id, service_access_token, auth_role, data_role, verification_role, source_root, oracle_python, source_oracle_secret, operation_contract_sha256, subjects, canonical_ids, tokens, cases, receipts. `service_access_token`必须由隔离Admin OAuth token端点按`client_credentials`签发，`sub`和`client_id`都等于`service_id`，且scope不超过该服务客户端的后台scope；fixture不保存或传递客户端secret。

Regular owned0600 file; database starts ink_auth_data_codex_test_, port differs from normal5433, directory starts /private/tmp/ink-auth-data-migration-. Every AUTH_DATABASE_URL/DREAM_DATA_DATABASE_URL/target_verification_url names same loopback host/port/database. Actual role/catalog and all five app role privilege flags false verified; only owner verifier reads data_directory. Reuse canonical immutable migration authority, do not replay migrations unnecessarily or broaden ACL. Source root read-only /Users/dmeck/.codex/worktrees/ef6e/ink-dream-memory; actual installed Python /Users/dmeck/project/ink-dream-memory/.venv/bin/python.

Exact operation_contract_sha256: 5967ae40f60858553f27f43dd83f021f93928e7c25c582d2e711d68df4d4e042. schema1/write/user dream:write/backgroundnull. identity1 1dc05e229d3f4147923fcdfe117c4c4930a43bf9f31439d7b4bc83d2a0d04cd3; unified0033 8b71cf5687f61dee884c3e6f2fb109c7a951b0789066a0f13583a7b67757fa71. No newDDL.

subjects/canonical_ids/tokens are strict six-key objects: user, other, read_only, matching_run, wrong_run, editor. Canonical IDs explicit decimal strings, OAuth subjects explicit primary-verified facts; NEVER derive canonical/subject from bearer strings. Matching Run persistence grant ties original owned Thread+Run/purpose/service. Wrong Run grant independently binds a different owned Run; Editor grant has only actual existing owned Editor Session purpose. none has no record key and sends empty bearer. New OAuth technical actors only with fresh explicit expiry; keys/credentials private and not exported. source_oracle_secret is explicit source-only constructor DI, not a production workflow authority secret or an issued token; it is sent only to the source child stdin.

## Cases

Strict keys per case: label, request_id, token, input, status, expected_code, expected_result, scenario, concurrent, replay. token is user/other/read_only/matching_run/wrong_run/editor/none; status200/400/401/403/404/409/503; scenario updated/missing_source/denied. Safe uppercase expected_code or null. Distinct labels/original request IDs. All success expected_result is independently prepared COMPLETE {updated,workflow_run_id,thread_id,message_id,error_code}; no fields omitted. Request strict {workspace_id,workflow_run_id,error_code}; error_code original truthy string (including whitespace if desired), no invented regex/max limit.

Each successful Run must already be authoritative FAILED with full valid lifecycle/source tuple/PG microseconds and current owned workspace. The component DOES NOT create a new FAILED transition. Preserve all original Run/error/parts/time/history/PF/maps/bindings/receipts and unrelated rows. Source role user/owner/time must match current Run. Null/non-null Voice/Agent metadata paths both required. No new Deck-enabled filter for failure persistence. Required updated raw metadata includes bigint, 1.0, -0.0, unknown nested Unicode and old claim fields; invalid/nonobject raw metadata decoding also exercise. missing_source uses a valid FAILED Run with original nonnull Thread/message pointers and missing message, expected updatedfalse, no repair/create.

At least one first-write concurrent success starts three same-original POSTs together before awaiting the first; at least one success replays same original afterward with complete output equality and no extra writes. Matching_run POST success mandatory. Denied read_only, wrong_run, editor, none mandatory with independently checked actual safe codes; body selectors strict400 INPUT_INVALID, current owner/source/tuple/time/corrupt/notFAILED and absent404/403/409 as actual production contract. An independent preseeded ORIGINAL receipt with different valid original error/input digest must cause POST409 OPERATION_REQUEST_CONFLICT; do not fabricate a second valid empty input or reset prior accepted data.

## Original GET checkpoints

Strict keys per receipt: after_label, request_id, token, query_tail, status, expected_code, state, source_label, expected_result, historical_later_lease. after_label names an executed case. state absent/committed/null. All200 committed require complete independent expected_result; usual recovery requires actual successful source_label with same original request ID. Unsuccessful/absent expected_result null and historical_later_lease false. GET OAuthwrite only; read_only/matching_run/editor403 mandatory. Other actor200 absent mandatory. Ordinary query_tail exactly &unexpected=caller ->404 mandatory; actor override exactly &actor_id=caller ->400 USER_OVERRIDE_FORBIDDEN mandatory; duplicate operation/source/error/codec/path selectors rejected. Independent bad original result/input digest GET409 OPERATION_REQUEST_CONFLICT mandatory; wrong current owner/deleted prior updatedtrue source/corrupt fields failures encouraged as explicit separate originals.

One historical_later_lease true committed GET required: primary separately seeds an older original receipt/audit/full completion/input SHA and current owned FAILED Run/source with later dispatchClaimId/dispatchClaimedAt already present. token user, source_label null, request_id must NOT match any current POST case. This is read-only existing-original recovery, NEVER a skip/continuation bypass. Verifier checks current ownership/tuple/source microseconds, full stored version/result/scopes/input digest reconstructed using originalerror/currentworkspace, and complete independent expected_result. GET must preserve all17 raw full relations, including later lease; no source patch or verifier writes.

## Effects and actual original source

Production POST/GET only. Verifier fixed17-table SELECT snapshots; AUTH/DATA used only public production paths and identity proof. Metadata success changes just target source metadata plus original receipt/audit (one each), all Run/transition/PF/maps/Session/Thread/order/source parts/time and unrelated full rows unchanged. Missing-source success changes only receipt/audit. Every rejected POST/GET full17 byte exact.

Entire original DreamLaunchFailureRecorder.record + WorkflowRunService.transition_run already-FAILED branch executes on fixed positional rows from current complete Run/source, not interpreted SQL: lookup read rollback, original alreadyfailed rollback, BEGIN, metadata SELECT/UPDATE, one independent COMMIT, close. Exact full metadata bytes/params and counters 1commit/2rollback/1close checked. The preceding actual Run FAILED COMMIT is preserved and not claimed newly executed here. Child -B environment only PATH+read-only source root, source-only secret and bodies private stdin; stderr/body/credentials never published.

## Primary run/fault/cleanup

Do not execute until producer static gate/readiness delivered. Wrapper injects INK_AUTH_DREAM_LAUNCH_FAILURE_FIXTURE, configured service/OAuth/auth-data secrets and only named isolated target creds privately, then node --import tsx tests/integration/adminDreamLaunchFailure.contract.ts. Preserve exact original fixture/expiry/failures/history. No normal-account/model/provider/Runtime/FS or75/76 public repeats.

Primary selected faults: target source metadata UPDATE, operation receipt INSERT, audit INSERT, each503 AUTH_SERVICE_UNAVAILABLE/full17 rollback while prior FAILED Run/transition commit retained. Inject a single lost response AFTER actual final COMMIT, initial503 -> original OAuth GET200 -> same-input POST replay200, full bounded result/metadata/source/digest/scopes, one receipt/audit and no duplicate effects. Use separate new operands/actors, no resets/cleanup of prior accepted history. Clean only named own injected trigger/function/process/pool objects; independent old125/fullrow preservation and all new17 fullrows/positive originals checks. Report actual command/exits/counts, initial setup failures separately, and normal business/model/Runtime/FS not executed.
