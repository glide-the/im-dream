# docs/design/notion-session — 目录合同

## 架构摘要

Notion 资源连接器产品、数据快照、用户凭证、Agent Runtime 交付和内置 Skill 的当前设计集。

## 文件清单

| 文件 / 目录 | 定位 | 职责 |
|---|---|---|
| `overview.md` | 历史总览 | Notion connector 四层抽象与 canonical snapshot 基线。 |
| `connector-interaction.md` | 产品—架构桥接 | 把当前 Settings/Chat 动作映射到单一 connector、轻索引、thread 投影和按需读取路径。 |
| `runtime-credential-and-skill-design.md` | 当前凭证/Runtime 设计 | agentdata 用户源、thread 轻索引投影、workspace Skill 索引、按需 page Read hook、Skill 内置、错误边界、三方案取舍和反过度设计评审。 |
| `runtime-credential-and-skill-sequence.md` | 当前业务时序 | 连接、Agent 启动、正常调用、失败降级、重新授权和断开流程。 |
| `upstream-gap-and-sync-review.md` | 上游同步决策 | 对比上游 `develop@e3523db9`、当前安全实现和 `ntn 0.15.1`，记录 data source 查询根因、reference 迁移范围与反过度设计评审。 |
| `resource-connector-er.md` | 数据关系 | connector、资源、snapshot 和 thread attachment 关系。 |
| `链接器概念的交互设计稿.pdf` | 视觉输入 | 连接器概念原始交互稿。 |

## 同步记录

- 2026-08-29：在 `.notion/README.md` 和每轮 workspace context 中恢复 `notion-session` Skill 的名称、标准入口、Runtime 发现别名和索引优先工作流说明。
- 2026-08-29：删除旧多工作台交互描述；新增完整发现分页、空范围撤销、LKG 与当前 selection 求交、公开 connector 最小投影、失败重授权保留有效凭证和无确认断开时序。
- 2026-08-28：canonical current snapshot 与凭证统一进入 actor `notion-runtime`；Settings 真实策略控制后台同步，保存资源立即首同步，Chat 仅投影到 thread workspace。
- 2026-08-28：明确凭证投影只发生在启用可信 thread workspace 的 Chat Runtime；Workspace Mode 关闭时不创建 `.notion-home`，Notion page hook fail closed，避免无 workspace sandbox 时 Agent 读取同用户文件。
- 2026-08-28：完成上游 `develop@e3523db9` 差异评审；保留 actor agentdata + per-thread light index，批准同步安全改写的 Skill references，并把正文读取收敛到 selected-ID Read hook。
- 2026-08-28：新增用户级 agentdata 凭证源、每 turn thread 私有投影、只读 page hook、backend-owned Skill 制品、错误降级、迁移/回滚、业务时序和反过度设计评审；删除旧 Bash/CLI/MCP Skill 指令，明确不再使用共享 process `~/.config/notion` 或 Agent Bash 直连 CLI。
