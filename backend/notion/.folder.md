# backend/notion — Folder Contract

## Architecture Summary
Notion resource connector backend package.
Dream owns the user-scoped connection lifecycle, PostgreSQL connector metadata,
server-owned agentdata credential/snapshot homes, scheduled synchronization,
and per-thread Runtime projections.
The pinned `ntn` CLI remains the authentication/API driver; it never chooses a
process-user home and is not exposed directly to the Agent.

## File Table

| File | Pos | Function |
|---|---|---|
| `__init__.py` | public-api node | Re-export Notion connector facade, auth helpers, persistence helpers, and canonical snapshot materializers for router/service callers. |
| `auth.py` | auth driver node | Runs `ntn login --no-browser`, hidden `ntn login poll`, and supported `ntn doctor` with an explicit provider-owned home and minimal environment. |
| `credentials.py` | actor agentdata provider node | Derives opaque actor roots under server agentdata, stages auth sessions, reserves the actor snapshot root, atomically promotes `auth.json`, and projects credentials to `{thread}/.notion-home`. |
| `errors.py` | error node | Shared safe connector/auth/credential/permission exception hierarchy. |
| `factory.py` | facade node | User-scoped orchestration for auth, discovery, immediate/explicit sync, policy transitions, actor snapshot publication, projection, and revocation. |
| `operations.py` | operations node | Read-only `ntn api` discovery/data-source query helpers plus the Runtime-only single-page Markdown call, all using an explicit server home; raw CLI output is classified and redacted. |
| `store.py` | PostgreSQL repository node | Persists connector metadata, selected resources, resource pages, snapshots, and thread attachments in the existing Admin-owned schema capability; stores no credential bytes or paths. |
| `sync.py` | sync node | Builds a paginated lightweight selected-resource index containing IDs and compact metadata only; materializes read-only thread `.notion/` index files without page bodies. |
| `snapshot_store.py` | actor snapshot provider node | Atomically publishes index-only `{actor}/snapshots/{connector}/current.json`, rejects newly embedded page bodies, and projects the current index into a validated thread `.notion/` without remote I/O. |
| `sync_policy.py` | sync-policy node | Validates default/desired/effective/revision/status, allowed intervals, due decisions, and safe transitions in existing connector config JSON. |
| `sync_scheduler.py` | scheduled-sync worker node | Periodically refreshes due authenticated connectors outside Chat; isolates per-connector failures without a queue or new schema. |

## Sync

- 2026-08-28: canonical current snapshots move to opaque actor agentdata beside credentials; resource save performs the first lightweight index sync, the strategy worker refreshes due indexes, Chat only projects the LKG, and page Markdown is fetched only by the Runtime Read hook.
- 2026-08-28: final agentdata credential audit keeps `auth.py` behavior unchanged while normalizing imports; explicit homes, minimal env, actor isolation, and thread projection remain the only supported path.
- 2026-08-28: align logical database selections with paginated `v1/data_sources/{id}/query`; snapshot production never calls the Markdown endpoint, which is reserved for a selected-page Runtime Read.
- 2026-08-28: replaced process-user/request-controlled `NOTION_HOME` with opaque actor-scoped agentdata homes, staged reauthorization, private per-thread projections, safe error mapping, and credential revocation. `ntn` remains the single backend driver; no Schema change or second Runtime protocol was introduced.

- 2026-07-04: initial Notion connector backend package added for connector CRUD, auth flow, resource discovery, snapshot persistence, and workspace attach.
- 2026-07-05: `auth.py` and `operations.py` added auth session-no-pending tolerance and `database` filter normalization for Notion `v1/search`; `factory.py` persisted `auth_session_*` lifecycle + poll idempotence to prevent auth status regression.
- 2026-07-05: `factory.py` now maps `No pending login session found`/`already consumed` to terminal `error` when no authenticated session exists, so frontend stops indefinite polling and surfaces actionable auth errors.
- 2026-07-08: `store.py` hydrates persisted selected resources onto connector rows as `sources`, and `operations.py` filters Notion People system data sources from database discovery.
