# backend/builtin_skills/notion-session — Folder Contract

## Architecture Summary

Built-in read-only Notion Skill. It may use only the internal `mcp__notion__*` tools and non-secret `.notion/` canonical snapshot; it never reads `NOTION_HOME`, token files, CLI output, or connector implementation paths.

## File Table

| File | Pos | Function |
|---|---|---|
| `SKILL.md` | skill manifest | Trigger rules, safe tool workflow, error handling, and read-only boundaries. |
| `references/notion-search.md` | search reference | Internal MCP title/type/sort/cursor search semantics. |
| `references/notion-page-read.md` | page reference | Internal MCP Markdown, metadata, and compatible block response semantics. |
| `references/notion-db-query.md` | database reference | Internal MCP data source filter/sort/cursor query semantics behind the compatible database ID. |

## Sync

- 2026-08-28: adapt the upstream search/page/database references to the internal MCP path, current data source query semantics, and Markdown response without reintroducing CLI or credential access.
- 2026-08-28: migrate from Agent Bash/`ntn` guidance to credential-isolated internal MCP tools.
