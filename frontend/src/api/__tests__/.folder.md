# frontend/src/api/__tests__ — Folder Contract

## Architecture Summary
Focused frontend API contract tests validate strict same-origin DTO parsing and client-owned projection helpers without introducing feature-owned fallback data.

## File Table

| File | Pos | Function |
|---|---|---|
| `gatewayModelsApi.test.ts` | gateway-model API contract test | Validate catalog safety and effective saved/Admin-default model selection. |
| `productApi.test.ts` | product API contract test | Validate Product BFF plan, subscription, usage, model, command, and payment DTOs. |
| `resourceConnectorApi.test.ts` | Notion connector API contract test | Verify fail-closed connector state plus strict real Hook/workspace operation entrypoints, MCP status, two installed Skill entries, stable file ID/path/MIME/size, and package revision projection. |

## Sync

- 2026-08-14: Added folder contract; `gatewayModelsApi.test.ts` covers new-user selection of Admin's validated callable default model.
- 2026-08-28: Added Notion connector transport coverage that rejects the retired browser-local connector authority.
- 2026-08-29: Notion transport coverage now validates the server-owned capability catalog and revision-aware safe Skill/file DTOs, including traversal-like path rejection.
- 2026-08-29: capability transport coverage now requires the real Read-hook and workspace-materializer sources/entrypoints instead of synthetic Skill operation rows.
- 2026-08-30: capability transport coverage preserves `notion-session` and `notion-cli` in server order with distinct execution availability.
