# frontend/src/router/__tests__ — Folder Contract

## Architecture Summary

Source-level tests for pure Story Workspace router seams.
Tests do not own browser history or duplicate runtime routing behavior.

## File Table

| File | Pos | Function |
|---|---|---|
| `storyWorkspacePath.test.ts` | `path contract test` | Verify canonical static/parameterized paths, root Chat default, queries, builders, and unsupported prefix rejection. |
| `storyWorkspaceChatBridge.test.ts` | `Chat bridge contract test` | Verify exact bound-Thread handoff state. |
| `storyWorkspaceDreamContext.test.ts` | `Dream context contract test` | Verify fixed Dream workspace context projection. |

## Sync

- 2026-08-31: root now resolves to canonical Chat and `/ink-and-memory/` is explicitly outside the route table.
