# backend/builtin_skills/notion-session — Folder Contract

## Architecture Summary

Built-in read-only Notion Skill. It uses only `Read` to navigate the current thread's lightweight `.notion/` index and request an indexed page through the controlled on-demand page hook; it never runs Notion MCP/CLI/Bash or reads credentials, token files, environment variables, or connector implementation paths.

## File Table

| File | Pos | Function |
|---|---|---|
| `SKILL.md` | skill manifest | Trigger rules, index-first `Read` workflow, on-demand page access, safe errors, and read-only boundaries. |
| `references/notion-search.md` | search reference | Locates pages by title, ID, or URL in the mounted lightweight index; not full-text search. |
| `references/notion-page-read.md` | page reference | Reads one indexed page through the Runtime hook and explains Markdown, metadata, and snapshot identity. |
| `references/notion-db-query.md` | database reference | Browses mounted database page IDs before requesting individual page Markdown. |

## Sync

- 2026-08-28: migrate from Agent Bash/`ntn` and Agent-visible Notion MCP guidance to the lightweight `.notion` index plus controlled on-demand `Read` path.
- 2026-08-29: align the folder contract with the actual `SKILL.md`: `Read`-only index navigation, selected-page membership checks, Markdown on demand, and Notion-local failure isolation.
