U10 Red evidence — 2026-08-06

1. OptionV2 page entry / confirmation origin

Command:
cd frontend && npx playwright test e2e/story-workspace-episode-execution.spec.ts --grep "mocked REST facts" --reporter=line --workers=1

Meaningful failure after the fixture was upgraded from legacy workflow.actionOptions to a
seven-item server-owned actionProjection:

expect(getByRole('button', { name: '生成镜头 Prompt' })).toBeFocused()
Expected: focused
Received: inactive

Cause found:
- a confirmation opened from the page did not select the recommended opaque actionId;
- a prior Agent-origin confirmation left return-to-Agent state behind, so a later page-origin
  cancellation reopened the Agent instead of restoring the page action.

2. Real narrow-screen action content fit

Command:
INK_REAL_EPISODE_QA=1 INK_REAL_EPISODE_WEB_BASE=http://127.0.0.1:5174 \
  npx playwright test e2e/story-workspace-real-episode-artifacts.spec.ts \
  --reporter=line --workers=1

Failure:
Expected every overflow action to satisfy button.scrollHeight <= button.clientHeight + 1.
Expected: true
Received: false

Cause found:
The fixed-height overflow grid shrank rows to 44px while three-line action content overflowed
into adjacent rows at 390px. Horizontal overflow alone was insufficient to detect it.

No real Episode action was dispatched during either investigation.
