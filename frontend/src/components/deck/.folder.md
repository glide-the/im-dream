# frontend/src/components/deck — Folder Contract

## Architecture Summary

Deck-specific presentation components. Server DTOs and mutations remain in `frontend/src/api/`, while the parent
`DeckManager.tsx` owns loading and action orchestration. Components in this folder must not invent Deck revision,
ownership, or capability facts.

## File Table

| File | Pos | Function |
|---|---|---|
| `DeckChatSelector.tsx` | Deck/Agent selector UI | Select a Deck and Agent only before a new conversation is fixed. |
| `DeckManagerPanels.tsx` | Deck task-mode presentation | Render the read-only use mode and creator management mode with semantic controls. |
| `DeckManagerPanels.css` | Deck task-mode styles | Apply Ink & Memory tokens, responsive layout, focus treatment, and 44px action sizing. |

## Sync

- 2026-08-15: Added explicit use/create Deck task panels; use mode can only hand selection intent to Chat, while
  creator mode exposes existing server-authorized writes. Aggregate version UI remains absent until capability data exists.
