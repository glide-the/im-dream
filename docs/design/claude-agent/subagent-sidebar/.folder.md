# docs/design/claude-agent/subagent-sidebar — Folder Contract

## Architecture Summary

Chat 子智能体任务可见性设计文档集。覆盖原始参考输入、产品需求、页面结构、交互层级、高保真 UI 映射，以及 parent/subagent 执行生命周期的实现结论。

## File Table

| File / Dir | Pos | Function |
|---|---|---|
| `source-brief.md` | source brief | 需求、组件挂载点与两张参考图说明。 |
| `prd.md` | product requirements | 数据真实性、安全边界、入口/详情面板和验收标准。 |
| `structure-sketch.md` | structure sketch | 桌面/窄屏页面结构与模块关系草图。 |
| `hierarchy-and-interactions.md` | interaction specification | 状态分组、开合、互斥、键盘与响应式逻辑。 |
| `ui-design.md` | visual implementation spec | CSS token、尺寸、动效及 React 组件映射。 |
| `implementation-notes.md` | implementation record | workspace transcript 投影、Agent 消息按钮与 foreground 生命周期修复。 |
| `assets/` | reference assets | 本地持久化的任务入口、详情与聊天任务按钮参考图。 |

## Sync

- 2026-08-04: 从临时 `files/` 归档到正式 docs 目录；参考图不再依赖系统临时路径。
- 2026-08-04: 记录异步 Agent completion 无 active parent consumer 的实测根因，并将 Agent/Task 强制 foreground 作为当前产品约束。
- 2026-08-04: 任务行升级为面板内详情入口；详情展示元信息、结果、错误和脱敏 activity 时间线，并明确 transcript 安全投影边界。
- 2026-08-05: “最新结果”保留 Markdown 块结构并复用 ChatMessageList 的 `ChatMarkdown`/prose 渲染链。
- 2026-08-05: 详情栏改用系统无衬线可读排版；左侧增加可拖动、可键盘操作且持久化的宽度分隔线。
- 2026-08-05: 详情架构升级为统一只读消息时间线；新增短 PRD Agent 与长 e2e Agent 两类参考图，明确 Agent 类型只改变数据、不分叉模板，并补充真实 messages 投影、legacy 降级、状态矩阵与验收清单。
