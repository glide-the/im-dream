# backend/claude_agent — Folder Contract

## Architecture Summary
Claude Agent 应用层，迁移自 Pawkeyland `application/claude_agent/`。  
提供流式 AI 写作助手的业务逻辑（会话管理、上下文组装、SSE 事件），  
依赖 `backend/libs/claude_agent_kit/` 提供 SDK 封装和类型。  
不依赖已退役的 legacy session runtime。

## Migration Source
Pawkeyland `application/claude_agent/`（已移除 pet/persona/mem0/necklace 专属逻辑）。  
Kit 层（types/runner/workspace）已移至 `backend/libs/claude_agent_kit/`。

## Two-Layer Architecture
```
backend/libs/claude_agent_kit/   ← Kit 层：SDK 封装、类型、工作区
backend/claude_agent/            ← 应用层：业务逻辑、会话编排（本目录）
```

## File Table

| File | Pos | Function |
|------|-----|----------|
| `__init__.py` | public-api node | 聚合导出（应用层 + kit 层 re-export）供 `server.py` 使用 |
| `observer.py` | observer node | `SessionLifecycleObserver`, `SessionObserverRegistry`, `LoggingObserver` |
| `admission.py` | resource-admission node | `ClaudeAgentAdmissionController` — env、构造与公开 replace 统一校验四项正安全整数及组合内存精确字节边界；既有并发/内存判断顺序与算法不变，替换只影响后续 acquire，不撤销既有 lease |
| `resource_observer.py` | resource-observer node | 透明 admission decorator 记录 grant/capacity/memory 结果、透传公开 config replacement，并通过既有 normalized EventBus 在旁路统计 completed/failed/cancelled；不保存 turn identity 或内容 |
| `resource_policy.py` | dynamic-policy provider/refresher | 精确 observer/Runtime capability 后严格读取 `system_settings` desired policy；四项采用正 JSON-safe integer 与组合内存边界，全局 effort 为 SDK 五值或 null；独立周期 task 只对更高合法 revision 请求公开 replace，失败同时保留 admission/effort LKG |
| `resource_diagnostics.py` | read-only resource-diagnostics node | 以同一安全规则投影 composition 已应用的 immutable admission + global effort effective state/version；config 替换与 admission 控制不在本模块，状态提交前继续报告整套旧快照；另独立采样 host、cgroup v2 与 Claude descendant RSS |
| `resource_postgres_sink.py` | resource-postgres synchronization node | 每 5 秒 capacity-1 latest publisher + 单 worker；statement/async timeout 后 capability-gated upsert 与 7 天 TTL，首个 `sampled_at=null` 显式 cast 后交由 DB clock 投影，错误只进入安全计数 |
| `tool_confirmation_store.py` | utility node | `ToolConfirmationStore`, `ToolConfirmationResult` — async Future 工具确认 |
| `thread_pool.py` | flyweight-state node | `AgentRunState`, `AgentRunStatePool`, `AgentRunStateSweeper` — TTL 会话池；支持在同一 RUNNING 所有权内计数一个持久化 Dream repair continuation，并持有 Settings/Editor 等既有 flyweight 状态。 |
| `context_builder.py` | context-assembly node | `ClaudeAgentContextBuilder` — 写作会话 system_prompt 组装；Settings SYSTEM_PROMPT 以低优先级 configurable block 渲染；prompt 模板中的字面 JSON 示例和 Prompt Architect `{{task}}` 占位符必须转义以避免 `str.format` 误解析；Session Retrieval Workflow 引导 Agent 用 `query` 字符模糊检索历史 session；`<workspace_context>` 存在时要求用 `workspace://files/...` 引用 Chat 可访问文件；精确前导 Skill 命令按 workspace 唯一真实目录做大小写 canonicalization；`<memory_context>` 存在时按 procedural-only Memory Workspace 规则提示 Agent |
| `workspace_context.py` | workspace-context-prompt node | `build_workspace_context_block` — 注入 `<workspace_context>` 提示；附加 workspace-local `.notion/` 轻量索引导航、snapshot identity、sync cursor，并直接消费 README 中由 capability catalog 动态生成的 Skill 索引；页面正文只在读取虚拟 page 路径时按需获得。 |
| `service.py` | core-business node | `ClaudeAgentService`, `ClaudeAgentRunRequest`, `ClaudeAgentTurnContinuation` — 保留 Phase 1/3、resume、SSE、Chat/Dream Session 与 thread-only TMPDIR；一个 allowlisted Dream slug mismatch 可在 assistant 落库前转换为已持久化 user 消息和正常 resume continuation，其余 Hook 失败 fail closed。 |
| `thread_retrieval.py` | chat-thread-retrieval service node | Plugin-style Chat history retrievers for `/api/claude-agent/threads` search: default fuzzy title/message matching, configurable env defaults, `auto` vector fallback, and `vector_query` interface-only placeholder. |
| `subagent_projection.py` | subagent-transcript projection node | 将 thread workspace 内 Claude Code `subagents/*.meta.json` 与有界 JSONL 首尾投影为任务状态、兼容 summary/activity 及 v2 安全只读消息时间线；消息包含有界 task dispatch、assistant Markdown、脱敏工具摘要、稳定顺序、终态与截断元数据，thinking 不下发。 |
| `event_bus.py` | event-bus node | `IEventBus` (Port), `InMemoryEventBus`, `BusProxyQueue`, `create_event_bus` — SSE 广播与重连 replay |
| `event_bus_redis.py` | event-bus-adapter node | `RedisStreamEventBus` — Redis Streams 适配器（多进程，默认不测） |
| `thread_factory.py` | factory-entry node | `ClaudeAgentThreadFactory`, `build_session_id` — 四阶段生命周期编排；同一 admission/lock/EventBus 可承载一次 server-owned Dream repair continuation，但每条消息仍经正常 assemble/runner/persistence；SSE 断开仅 unsubscribe，stop/cancel 统一收敛。 |

## Persistence Contract (aligned with better-chatbot)
**2026-06-09 P2 重构**：`_persist_turn` 拆分为三个独立方法：
- `_persist_user_message`：在 `execute_session` 调用 `runner.run_streaming()` **之前**立即写入用户消息（防止 thread 切换丢失）
- `_persist_assistant_turn`：turn 成功完成后写入 assistant 消息 + 更新 `claude_session_id`
- `_persist_partial_assistant`：SSE 流被取消/出错时 flush 已收集的部分 parts（metadata 标 `is_partial=true`）
- `_persist_turn`：保留为遗留别名，依次调用 `_persist_user_message` + `_persist_assistant_turn`

原有持久化字段不变：
- **user**：原始文本 + `parts_json`（含附件等 AI-SDK parts）
- **assistant**：`parts_json` 包含所有 `reasoning` + `tool-invocation` + `text` parts，`metadata` 含 usage/model/toolCount/is_partial

每次成功 turn 后，`_persist_assistant_turn` 还会将 `result.session_id` 和 `_AGENT_RUNTIME_CONTRACT_VERSION` 写回 `chat_thread`，下一轮 `assemble_context` 据此决定是否 resume。

## Key Constraints

- [Sync] 2026-09-01: Dream auto-repair first commits one stable user `chat_message`, then emits `chat-message`, then reruns normal context assembly/Runner resume under the existing Factory. A persisted dispatch claim elects one continuation across duplicate callbacks; the second Hook failure is terminal and no third turn is possible.
- [Sync] 2026-09-01: `service.py` uses the existing actor/thread Notion
  credential projection as the platform activation fact, refreshes common plus
  selected builtin Skills, and rewrites only their exact directory-source
  sandbox read paths before Runner launch.
- [Sync] 2026-09-01: `context_builder.py` only canonicalizes an exact leading slash command when one real `workspace/skills/<id>/SKILL.md` case-insensitively matches, allowing `/Skill-Creator` to resolve to canonical `skill-creator` without rewriting prose, plugin commands, or ambiguous/missing entries.
- [Sync] 2026-08-31: `thread_factory.py` 保留 Dream 绑定完整性失败的稳定错误码，但向客户端发送异常提供的用户安全正文；结构性冲突为 `retryable=false`，不再把内部码拼入展示文本。
- [Sync] 2026-08-31: legacy session runtime 已删除；Claude Agent 是 Voice/Writing/Chat 的单一 Thread SSE 路径。
- [Sync] 2026-08-30: attached Notion workspace context consumes the materialized README Skill section, so `notion-session`, `notion-cli`, future catalog entries and their availability share one dynamic source of truth.
- [Sync] 2026-08-29: Editor write results unwrap stdio business envelopes before SSE/history persistence, refresh only the actor's live session cache, and never publish a success event for `ok:false`.
- [Sync] 2026-08-28: `service.py` projects the current actor's Notion credential and index-only snapshot only after server workspace validation; Chat initialization performs no Notion remote I/O, and the Runtime Read hook's local failures preserve ordinary Agent conversation semantics.

- [Sync] 2026-08-25: MCP state 每个 new/resume turn 按 canonical actor 从 `dream_mcp_*` 快照读取；不再同步 Claude CLI config/secure store。含 secret 的 SDK 配置由 runner 在精确 thread `.claude-tmp` 内短暂投影，Workspace Mode 关闭也不改变数据库事实来源。
- 不依赖第二套 legacy session registry；路由由 FastAPI composition 注册
- Kit 层导入：`from libs.claude_agent_kit import ...`
- `session_id = user_id`（无 persona 维度；可扩展为 `user_id__tag`）
- `context_builder.py` 的 system prompt 模板只允许 `{recent_sessions_block}` 作为格式占位符；其他字面 `{}`（例如 JSON 示例）必须用双大括号转义。
- [Sync] 2026-06-06: Memory Workspace 初始化边界调整：`service.py` 不再调用 `init_memory_workspace()`；`context_builder.py` 仅在 `/memory/` 已存在时注入 procedural-only `<memory_context>`。
- [Sync] 2026-06-09: `context_builder.py` 系统提示新增 Planning Prompt Optimization Workflow；规划任务先按 Expert Prompt Architect 模板生成 Optimized Prompt，再基于该提示规划执行。
- [Sync] 2026-06-09: `service.py` 从 `system_config` 读取 `im_full_access_enabled`，把 Settings「应如何批准 IM」完全访问开关注入 Claude Agent Runner 权限策略。
- [Sync] 2026-06-09: P2/P3 fix — `service.py` `execute_session` 拆分持久化为三阶段；`routers/claude_agent.py` 新增 `GET /threads/{id}/status` 端点。
- [Sync] 2026-06-09: EventBus Port/Adapter — `event_bus.py` + `event_bus_redis.py`；`thread_factory` SSE 断开不 cancel bg_task；`GET /threads/{id}/stream` 重连端点。
- [Sync] 2026-06-13: `service.py` 在 cwd 解析前读取 `workspace_enabled`，让首次 workspace 初始化即写入正确的 Claude Code Bash sandbox 设置；client-provided cwd 不再覆盖 server-owned thread workspace。
- [Sync] 2026-06-13: `service.py` 移除 `assemble_context` 内部的 `import database as _db`，避免遮蔽模块级 `_db` 导致 system_config 加载失败。
- [Sync] 2026-06-13: `service.py` 将 runner 内部 `tool_input_delta` 转发为 SSE `tool-input-delta`，用于内置 `Write` 工具终端式写入预览；delta 仅用于 live preview，不进入持久化 parts。
- [Sync] 2026-06-14: `service.py` 在 editor MCP 写工具成功 `tool_result` 后发布 Edit Session `session_updated(source=agent, toolCallId)`，前端据此替代 2 秒盲等。
- [Sync] 2026-06-16: `context_builder.py` 的 Session Retrieval Workflow 更新为 `query` 优先的字符模糊检索说明，并标明 vector 模式只是接口边界。
- [Sync] 2026-06-17: `service.py` 的 SSE errorText 现在会拼接 runner 异常 notes，确保 `apply-seccomp` 等 sandbox 诊断提示可直接返回前端。
- [Sync] 2026-06-21: `service.py` 从 `system_config` 读取 `sandbox_network_mode` / `sandbox_network_allowed_domains` 并传给 workspace 初始化。
- [Sync] 2026-06-22: `context_builder.py` renders Settings SYSTEM_PROMPT as a lower-priority configurable block; `service.py` loads it through `get_system_config`, rebuilds cached prompts when it changes, and skips workspace/cwd initialization when Workspace Mode is disabled.
- [Sync] 2026-08-22: `service.py` separates Workspace Mode from Claude CLI temp ownership: disabled mode still keeps `cwd=None`, clears `state.cwd`, and injects no workspace context, while `AgentRunOptions.claude_tmp_workspace` binds the CLI to the current thread's runtime-only `.claude-tmp`.
- [Sync] 2026-06-25: `thread_factory.py` adds bounded `stop_thread()` cancellation for frontend stop requests; `service.py` emits `finish` + sentinel after partial persistence on cancellation so EventBus readers close.
- [Sync] 2026-08-26: `admission.py` 的 cgroup effective headroom 在 raw headroom 上保守加入 `inactive_file` 与 `slab_reclaimable`；不计 `active_file` 或不可回收 slab，避免 AutoDL 文件缓存造成假性拒绝，同时保留真实低内存 fail closed。
- [Sync] 2026-08-28: 四项 config 在 env、desired parser、公开 replace 与 diagnostics 统一为 `1..9007199254740991`，run+reserve MiB 不超过 `8589934591`；拒绝 0/null/bool/string/fraction/overflow/sentinel，移除旧产品边界和 int4 漂移。
- [Sync] 2026-08-28: composition callback 仅为合法更高 revision replace；same revision/same config 不重复 replace但更新 diagnostics loaded/status，rollback/same-different/invalid/unavailable 保留 LKG；Observer、service、Runner、ThreadFactory、状态机、lease 与 SSE 不变。
- [Sync] 2026-06-27: `thread_retrieval.py` adds the plugin registry for Chat history search over thread titles and persisted message text; vector retrieval remains interface-only.
- [Sync] 2026-07-04: `workspace_context.py` appends workspace-local Notion connector metadata from `.notion/` canonical snapshot files; `service.py` materializes the canonical snapshot into the workspace before user-message assembly.
- [Sync] 2026-07-05: `workspace_context.py` now renders connector ID and sync cursor alongside the attached `.notion/` snapshot identity so the prompt summary mirrors the canonical snapshot more explicitly.
- [Sync] 2026-07-20: claude-todo 落地 — `service.py` 新增内存态 `TodoState`（挂 `AgentRunState`）、v1 `TodoWrite` 流内捕获（`_observe_todo_write`）、`todo-updated` SSE 发射（不入 `collected_parts`，`INK_AGENT_TODO_MAX_ITEMS` 截断置 `truncated:true`）、`on_tasks_changed` v2 回调与 `build_thread_todos_payload()` REST 助手；`thread_pool.py` 的 `AgentRunState` 增加 `todo_state` 内禀字段并在 `snapshot()` 暴露。
- [Sync] 2026-07-23: `service.py` passes `sandbox_network_allowed_domains` from system_config into `AgentRunOptions` and transparently forwards `confirmationKind` / `networkRequest` from the runner confirmation payload onto the SSE `tool-approval-request` frame (SandboxPermissionRequest, claude-agent-sandbox-network-permission-tool.md §5/§6).
- [Sync] 2026-07-26: `service.py` 移除向 `AgentRunOptions` 传 `sandbox_network_allowed_domains`（该值仍用于 workspace 初始化 → CLI sandbox settings.json）；`confirmationKind`/`networkRequest` SSE 透传保留（can_use_tool 路径）。
- [Sync] 2026-07-26: `service.py` 读取 `sandbox_fs_allowed_write_paths` 并传入 `get_or_create_workspace`（与 sandbox_network_allowed_domains 同一 plumbing 模式）。
- [Sync] 2026-08-04: `subagent_projection.py` 新增 thread-scoped subagent transcript 投影；区分 running/completed/failed/cancelled，并只扫描 JSONL 的有界首尾内容。
- [Sync] 2026-08-05: `subagent_projection.py` adds projection v2 readonly messages with deterministic ids/sequences, credential-key redaction, bounded tool summaries, task/final/status events, and legacy summary/activity compatibility.
- [Sync] 2026-08-12: `service.py` uses the existing runner `on_message` callback to persist the SDK-native Session ID immediately for the shared Chat/Dream Thread lifecycle; successful-turn persistence remains as an idempotent safeguard and resume resolution is unchanged.
- [Sync] 2026-08-13: `service.py` assembles a server-scoped `DreamArtifactTurnTicket` and calls a named `DreamArtifactTurnHook` only after successful root turns; `context_builder.py` directs Dream output to canonical workbench files while the host owns `.dream` synchronization. Runner, SSE and Claude session identities are unchanged.
- [Sync] 2026-08-14: Dream surface 初始化时部署 `.dream/WORKBENCH.md` 与 `.dream/ASSET-COLLABORATION.md`；`service.py` 在每个可信绑定 turn 刷新并注入两个实际路径，同时选择既有 Deck `dream_mode`，避免普通 Chat standalone proposal JSON 覆盖 canonical 文件操作。公开 Chat DTO、runner 入口和 session 语义保持不变。
- [Sync] 2026-08-13: Dream 根 turn 成功 Hook 在文件发布和 Episode 绑定后幂等刷新 PostgreSQL Story Project 投影；`service.py` 只记录内部同步状态，不改变 SSE、终态或 session。
- [Sync] 2026-08-13: `service.py` normalizes structured Editor MCP `ok:false` results to tool errors before live SSE/history persistence and suppresses editor refresh/session events on failure.
- [Sync] 2026-08-22: `context_builder.py` 在既有 engine system prompt 生命周期内定义 `workspace://files/...`，且 Workspace Mode 关闭、无 `<workspace_context>` 时禁止输出该协议。
