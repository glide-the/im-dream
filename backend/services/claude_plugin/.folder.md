<!-- [Sync] 2026-09-16: route catalog/install/operation persistence through Registry175-182 and retire the Dream Marketplace SQL reader. -->
<!-- [Sync] 2026-09-15: workspace_packer accepts a lazy Registry106 metadata loader while retaining artifact and filesystem ownership. -->
<!-- [Sync] 2026-09-16: remove the zero-caller Deck refs SQL service after public routes adopted Admin DTOs. -->
<!-- [Input] Existing install/artifact/CLI algorithms and Admin prepared installation metadata. -->
<!-- [Output] Shared local verification ownership; database migrations remain domain-specific. -->
<!-- [Pos] Claude Plugin service folder contract. -->
<!-- [Sync] 2026-09-15: artifact/CLI verification methods are static and reused by Admin refs consumer without a DB object. -->
# backend/services/claude_plugin — Folder Contract

## Architecture Summary

Owns the production Claude Code plugin install, artifact verification, and
Admin-approved Remote Marketplace consumption boundary. Deck-reference persistence uses Admin strict DTOs while Dream retains artifact and CLI verification. Remote Marketplace
governance and DDL stay in Admin; Dream never creates schema or treats local
paths/constants as the new managed catalog.

## File Table

| File | Pos | Function |
|---|---|---|
| `install_service.py` | real install executor | Accept an Admin lifecycle reporter; run CLI/Git/digest/artifact work and emit strict begin/progress/fail/complete evidence. Remote entry installs add/update `remote_url[#ref]`, verify managed checkout origin/commit/manifests, and compare the complete plugin digest before Admin commits ready state. |
| `digest.py` | canonical digest facade | Re-export the single plugin-tree digest shared with the Claude Agent kit and artifact store. |
| `cli.py` | Claude CLI boundary | Execute the configured real Claude binary in the server-managed runtime and capture bounded evidence. |
| `workspace_packer.py` | shared filesystem pack executor | Read frozen manifest before metadata I/O；fresh public Chat uses Registry106 loader，internal compatibility callers may still supply DB；artifact digest、copy、init、surface、repair和manifest均留在Dream。 |

## Sync

- 2026-08-19: Added the capability-gated platform-global Remote Marketplace reader and exact entry lineage. New remote installs do not consult `KNOWN_MARKETPLACE_LOCAL_PATHS`; the old manual package-spec branch remains compatible until a separate contract removes it.
- 2026-08-19: Approved entry receipts include branch/tag ref, commit, raw manifest hashes, and canonical full-plugin digest; any drift fails closed before a ready installation.
- 2026-09-16: `deck_refs_service.py` was removed after public list/prepare/replace had moved to `AdminDeckRefsData`. The local verifier still checks Admin-prepared artifact bytes and Claude CLI compatibility before replace; it has no database fallback.
- 2026-09-16: `marketplace_service.py` and install lifecycle SQL were removed. Registry175-182 now owns catalog reads, operation/installation queries, locks, soft uninstall and atomic ready persistence; Dream keeps the unchanged CLI, Git, digest and immutable artifact algorithms.
