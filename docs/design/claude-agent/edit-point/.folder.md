# docs/design/claude-agent/edit-point — Folder Contract

## Architecture Summary
Claude Agent 与编辑器会话集成的模块化设计文档集。  
以三层架构为核心：EditorEngine 作为 MCP 资源服务器，EditorState 通过适配器映射到文件系统工作空间，  
UI/Hooks 层作为人类与 Agent 的协作面，涵盖操作确认、可视化与多 Agent 冲突仲裁。

## File Table

| File | Pos | Function |
|------|-----|----------|
| `edit-point-event-system.md` | overview | 三层架构总览：文档对象模型（档案>片段>评论）、EditorEngine / EditorState / UI 层职责、设计原则、模块索引 |
| `mcp-tools.md` | design-doc | EditorEngine MCP 工具目录：8 个工具完整 Schema（list_segments, read_segment, write_segment, delete_segment, list_comments, read_comment, reply_to_comment, read_session_meta）、权限矩阵、写工具确认流时序图 |
| `workspace-adapter.md` | design-doc | EditorState → 工作空间文件系统适配器：目录结构、文件格式规范（manifest.json / segments / comments）、同步策略、读写路径分离 |
| `human-agent-collab.md` | design-doc | UI/Hooks 层人机协作设计：Agent 操作可视化、确认流程（SSE 事件结构、Approve/Reject）、AgentActionOverlay / AgentOperationHistory 组件设计、对象泳道图 |
| `conflict-resolution.md` | design-doc | 多 Agent 冲突解决策略：冲突检测（片段隐式锁、版本号）、冲突信号传递（SSE tool-conflict-detected）、人类仲裁 UI 与流程、锁失效清理、ISSUE 生命周期类比 |
