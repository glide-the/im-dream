# backend/builtin_skills/notion-cli — Folder Contract

## Architecture Summary

Backend-owned Notion CLI Skill imported from `glide-the/ink-and-memory` `develop` commit `e3523db9f07400736123d2361111f428e60db0e4` and renamed from `notion-session` to `notion-cli`. It documents direct `ntn api` usage and remains a separate package from the index-first, Runtime-hook-backed `notion-session` Skill. The shared built-in Skill refresher copies both packages into every full thread workspace without introducing another synchronization path.

This package does not change Dream's credential boundary: actor Notion credentials remain server-owned and are not merged into Agent/Bash environment variables by this directory.

## File Table

| File | Pos | Function |
|---|---|---|
| `SKILL.md` | skill manifest | Trigger rules, prerequisites, command selection, and direct `ntn api` workflow under the `notion-cli` identity. |
| `references/notion-search.md` | CLI search reference | Documents `ntn api v1/search`, filtering, sorting, pagination, and response fields. |
| `references/notion-page-read.md` | CLI page reference | Documents Markdown, metadata, block, and property reads through `ntn api`. |
| `references/notion-db-query.md` | CLI database reference | Documents database schema lookup, filters, sorting, pagination, and response fields. |

## Sync

- 2026-08-30: import the upstream `.claude/skills/notion-session` package from commit `e3523db9`, rename its directory/frontmatter/title to `notion-cli`, and keep all three reference guides with source provenance.
