# backend/services/admin_gateway — Folder Contract

- 本目录只通过已认证的 Admin Gateway 公共协议取得模型、推理与结算能力；Dream 不直接查询 Admin 模型表，也不拥有 Drizzle migration。
- `models.py` 严格解析 catalog 闭集。`context_window/max_output_tokens` 保持 Gateway 公开能力语义；`claude_code_auto_compact_window/claude_code_max_context_tokens` 是 server-only nullable Runtime 配置，不进入 `public_dict()`。
- `selection.py` 根据 canonical user、保存偏好、callable catalog 和客户端一致性检查返回最终 `GatewayModel`；alias wrapper 仅为兼容投影，不能丢失生产 turn 所需 Runtime metadata。
- Runtime 数值只接受 `1..2_147_483_647`；未配置返回空 env 投影。目录不接受浏览器、Deck、Plugin、用户环境或任意 env key。

## Sync

- 2026-08-28: Full model selection now carries the two optional Claude Code Runtime windows through the authenticated catalog boundary.
