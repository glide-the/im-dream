# frontend/src/components/deck — Folder Contract

## Architecture Summary

Deck-specific presentation components. Server DTOs and mutations remain in `frontend/src/api/`, while the parent
`DeckManager.tsx` owns loading and action orchestration. Components in this folder must not invent Deck revision,
ownership, or capability facts.

## File Table

| File | Pos | Function |
|---|---|---|
| `DeckChatSelector.tsx` | Deck/Agent selector UI | Select a Deck and Agent only before a new conversation is fixed. |
| `DeckManagerPanels.tsx` | Deck home/management presentation | Render user published-clean shortcuts plus default-visible system Decks, persistent Available/System launcher groups, the read-only Deck preview page, and the Work full inventory, switches, More menu, and related-Chat cleanup dialog. |
| `DeckManagerPanels.css` | Deck launch/management styles | Apply Ink & Memory tokens to complete spaced launch icons, neutral ink-and-paper preview hero/examples/info rows, static system rows/cards, Work rows, same-DOM narrow layouts, focus, reduced motion, and 44px targets. |
| `DeckVersionPanel.tsx` | folded content-version history | Render immutable Deck vN as the primary timeline and runtime binding history as a secondary disclosure. |
| `DeckVersionSubmitDialog.tsx` | explicit commit confirmation | Present server diff/source/target/impact and create vN only after confirmation. |
| `DeckPluginVersionPicker.tsx` | exact-version confirmation | Select a server-adjudicated exact version and explain next-run scope and conflicts. |
| `DeckPluginVersionCard.tsx` | version option row | Present exact semver, current/release state, compatibility, and recovery facts. |

## Sync

- 2026-08-16: PDF pages 1-2 own the outer home; explicit Deck vN commit and folded immutable history are primary, runtime semver is secondary.
- 2026-08-17: Deck main filters user Decks to explicit published-clean enabled versions while system Decks bypass enable/disable and default to visible; Work remains the only full inventory and switch surface.
- 2026-08-17: Work More adds a Deck-filtered related-conversation preview, production Chat deletion, and guarded Deck deletion recovery.
- 2026-08-17: Deck launcher splits user Available Decks from System Decks and keeps both type groups visible with empty states; registered default Decks can open the shared read-only preview page but cannot be edited or changed from this UI.
- 2026-08-17: Deck preview uses a theme-token-only graphite and paper treatment; persisted Deck accent colors do not tint the preview hero or icon.
- 2026-08-17: Preview rows expose one typed orchestration callback plus launching/error states; the parent dispatches Chat examples to Chat and Dream examples to the dedicated workbench.
