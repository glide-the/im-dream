<!-- [Input] Registered77 failure envelope, exact old76 parity and actual original recorder/source contracts. -->
<!-- [Output] Primary-owned named technical target, full-source public verification and atomic recovery plan. -->
<!-- [Pos] Cross-project acceptance coordination; no normal-account, model or Runtime completion claim. -->
<!-- [Sync] 2026-09-15: plan independent envelope verification after the prior authoritative Run failure commit. -->

# 启动失败持久化公开契约与恢复验证

## 背景与问题

Admin 已注册 `dream-launch-failure.envelope`，schema1/write/dream:write/backgroundnull，输入仅 workspace_id/workflow_run_id/error_code，输出完整 updated/workflow_run_id/thread_id/message_id/error_code。确认的 SHA 为 `5967ae40f60858553f27f43dd83f021f93928e7c25c582d2e711d68df4d4e042`；依赖现行 identity1 与 unified0033，不新增结构。原76全部描述符及独立 Preflight receipt/delegation 均保留。实际首轮 registration11、whole tsc/eslint、Python AST、Next-config4、diff 均 exit0；这不等同公开 PostgreSQL 通过。

原 DreamLaunchFailureRecorder 先调用 WorkflowRunService.transition_run 提交 failed，随后独立事务更新源消息 metadata。新的接口只接管第二个事务，不能用接口成功声称 Runtime 已执行或新 Run failed 转换已验收。原始 JSON 的 bigint、1.0、-0.0、Unicode、未知字段，消息 parts/time/Thread/Run 指针，以及较晚 dispatch lease 必须保留对应语义。

## 目标与边界

Root 负责具名 target、身份/凭据、fixture、fault/真实最终 COMMIT 响应丢失、历史全行核验和安全归档。Admin 任务负责 frozen77 的生产 Route/Service/Repository/固定 codec、Original GET、strict harness 和 READY；Dream 任务随后接入公开入口，保留业务编排与两次原事务顺序。Luna 仅运行已准备公开生产入口与 SELECT-only verifier 及确定性静态检查，不 seed、不 DDL、不读取公开凭据、不改产品或断言。

新 `ink_auth_data_codex_test_792494523a17_failure77` 仅从本轮已验证可删除 `_workspace76` 克隆；拒绝既有目标及 source 活跃会话。每次特权操作核验 owner、127.0.0.1、51534、已绑定 data_directory。正常5433数据库禁止技术操作。Admin 唯一 Drizzle 既有 capability 和受限 ACL 复用，不重放 migration，不引用全局最新 head。私有配置/fixture/seed 快照均 regular owned0600；新的短期 OAuth 仅在 producer READY 后签发。

## 概念与规则

- 成功 fixture 的 Run 已由主任务在隔离目标独立准备为 failed，具有现行全列、合法源 tuple 和微秒时间。公开接口不新建 Run/转换/Session/Thread/消息，也不修改已提交 Run。
- first POST 同原 request 至少三次共同发出、未 await 首次写入；replay 全字段一致，每 distinct original 仅一个 receipt/audit。匹配 Run 的 server-persistence 可 POST；错误 Run、Editor、readonly、缺失 token 均按生产边界拒绝。
- OAuth subject 与 canonical bigint decimal string 来自主任务显式核验映射，不能解码 token 推 actor；任何外部 user_id/actor/SQL/table/path/codec selector 拒绝。
- 原全 recorder/transition source 在 fixed positional rows + 可控 clock/UUID/get_db DI 上运行，原 already-failed read rollback、独立 metadata COMMIT/rollback/close 和完整参数与公开结果对照。source adapter 不解释 SQL，不打开连接池，不访问 Gateway/模型/文件系统。
- missing source 返回 updatedfalse，保持全部消息行不变；无修复/自动建表。invalid/nonobject metadata 按原固定 codec 行为处理，完整结果不省略 null。
- Original GET 只 operation/request，OAuthwrite/currentowner、完整 version/result/thread/run/editor scopes 与原输入 SHA。其他 actor 得 absent；query actor override400、普通额外参数404。独立负 original digest409；较早历史 completion 在已有较晚 lease 下仅只读恢复，不覆盖该 lease。
- 所有 rejected POST/GET 完整17关系逐行字节相等；success 仅目标 source metadata + receipt + audit，可证明原 failed Run、transition、PF、消费、Session、Thread、发布绑定、parts/time 与所有无关行保留。
- 公开 exit0 后 Root 用独立新 operands 选择 source metadata UPDATE、receipt INSERT、audit INSERT 的故障，各503且全17rollback；一次实际最终 COMMIT 执行后丢响应，初始503→原 GET200→sameinput replay200，完整 source/rawJSON/digest/scopes/单效果保留。只清理本轮具名 trigger/function/pool/进程，不清理既有验收记录。

## Optimized Prompt:

You are the primary coordinator of the frozen registered77 independent Dream launch failure persistence public acceptance. Evidence confirms the exact strict DTO and SHA, unchanged complete old76 catalog and separate Preflight/delegation bytes, provider-free registration11 and whole type/lint/AST/Next-config/diff exit0. Read the actual public harness, strict private fixture recipe, original DreamLaunchFailureRecorder.record and WorkflowRunService.transition_run, and reuse the established owner-verified technical target/configuration wrappers. Consult only primary Drizzle transaction and PostgreSQL locking/error documentation already linked in the preceding validation plans. Preserve all user/other-agent work and prior accepted complete rows/receipts. This component owns metadata persistence after a separately committed failed Run; it does not own Runtime or a new failed transition.

Create a new explicitly named disposable failure77 target only from the already owned workspace76 technical target, refusing existing target or active source sessions and verifying owner/loopback51534/data_directory before privileged actions. Record all125 source table fingerprints before/after. Reuse Admin-only immutable Drizzle capabilities and restricted AUTH/DATA ACL without migration replay or normal database mutation. Prepare fixed new subjects/canonical IDs, independently owned full Workspace/Thread/source/failed Runs/transition facts, null and nonnull Voice paths, malformed/nonobject/raw JSON cases, missing source, wrong-owner/tuple/time/not-failed inputs, an independent conflict original, a preexisting historical completion with a later dispatch lease and only legitimate purpose-bound grants. Use actual source/schema fields, explicit closed token-label mappings, canonical decimal IDs, strict complete DTO expectations and owned0600 private artifacts. Do not sign short-lived OAuth or execute public POST until the producer delivers the actual public static gate and READY.

Delegate one already-prepared nondestructive public Route/GET/SELECT contract to the existing Luna runner with exact cwd/command/exit/safe raw receipts and cleanup boundary. Require first same-original concurrency, full actual original recorder parameters and ordered commits/rollbacks, all17 full relation comparisons, complete results/digests/entity scopes, permissions and closed-query behavior. Retain every original failure; fix a demonstrated harness/product defect without reset, skips or weakened assertions. After public0, primary alone injects three narrow owned faults and a real final-COMMIT response loss on new operands, confirms full rollback or exact committed original recovery/replay without duplicate effects, and cleans only named own objects. Independently verify every preseed source125 row and every accepted new17 row, scan all raw artifacts for actual private values, archive pinned-SHA public receipts, safely merge coordinator docs and check Markdown references. Leave whole migration and normal Google/account/model/runtime/no-PG verification goals active until their actual required results exist.

USER REQUIREMENT:
遵从 DTO/ORM 将 Dream 启动失败元数据事务迁入 Admin，保持 failed 生命周期的原提交顺序、权限、JSON与并发语义，通过公开入口和真实提交恢复验证；技术隔离结果不冒充真实业务。

## 流程、状态与接口

| 状态/步骤 | 执行模块 | 输出与失败处理 |
|---|---|---|
| 已 failed | Admin 原 Run.fail/独立 fixture | 本接口只读取原状态；非 failed409 |
| 校验 source | Admin Service/typed Repository | 当前 Workspace/Run/Thread/actor/time tuple 一致；不符拒绝 |
| 新 envelope | 固定 codec + receipt UOW | 改 metadata、receipt/audit 原子提交；missing source updatedfalse |
| 原请求重放 | 原 receipt/current事实检查 | 全结果恢复，不重写已有 metadata/较晚 lease |
| unknown commit | Dream 统一客户端/原 GET | 保留原 request；无证据不盲重试新写入 |

## 验收顺序与命令

| 用户流程/检查 | 数据与权限 | 成功标准 | 失败/恢复 |
|---|---|---|---|
| failed 后保存错误 | 现有 owned Run/source | 原已提交 Run 保留，metadata/source与实际原 recorder 一致 | 三处全 rollback，原 Run 保留 |
| 并发/重试 | same original+OAuth/idg | 一份全结果、一个 receipt/audit | input conflict409 |
| 回执/较晚 lease | OAuthwrite/current owner | GET 全行不变、较晚 lease 保留 | wrong scope/owner/digest 拒绝 |
| source missing/非法 | 原 fixed codec及 tuple | updatedfalse不创建；正常非法路径 safe error | 完整17无意外变化 |
| 全历史保留 | 原125、新17全行 | source before/after、accepted originals exact | 不reset，保留失败诊断 |

拟执行 `python3 /private/tmp/ink-auth-migration-validation/prepare-failure77-target.py`（cwd Admin729f），随后 strict fixture/static gate、公开 wrapper、primary atomic/preservation，具体运行回执到 exec 目录安全归档后再引用。官方资料：[Drizzle transactions](https://orm.drizzle.team/docs/transactions)、[PostgreSQL explicit locking](https://www.postgresql.org/docs/18/explicit-locking.html)、[PostgreSQL error codes](https://www.postgresql.org/docs/18/errcodes-appendix.html)。主要风险是把独立 metadata 事务错误并入 failed 转换、raw JSON 数值舍入、当前归属变化、source微秒不匹配、恢复时覆盖较晚 lease、并发 receipt 重复和 token 等待过期；不新增业务限制、控制服务或 Runtime/FS行为。

## 当前阶段

target 与 fixture 已实际通过；首次公开 exit1 的错误码先后预期已实际诊断，6个已提交正例独立核验通过。16负例/27 GET续接、atomic/preservation与安全归档尚待实际命令，整体目标 active。Google、正常账户/实体、Gateway/真实模型、完整运行链、共享文件系统和 Dream 全入口无 PostgreSQL 运行证据均尚未因此完成。

## Target 实际回执与执行窗口

主任务实际 `python3 /private/tmp/ink-auth-migration-validation/run-prepare-failure77-target.py`（cwd Admin729f，底层 prepare-failure77-target.py）exit0；exec session42187/chunk2c0191。新 failure77 来自本轮 owned workspace76，owner/127.0.0.1/51534/data_directory 一致，source125表 before/after exact，fingerprint SHA `0939609b17dd1ecdd410b3fac5b529092218c0f8bf16634c1fe83e8a94ac66e7`；ACL apply0、migration 未重放、正常数据库未触碰。ledger61仅为这一已owned快照事实，不作为 Dream 全局 head 依赖。

Luna首轮 AST通过但 harness 错误要求字面 active 导致exit1；脚本实际 pg_stat_activity 排除当前PID并断言会话数零，未因此修改产品。focused correction AST/text0；首轮失败和修正命令的 shell quoting失败均保留在独立raw回执。静态条件存在不能替代上述实际 target 验证。

Producer 实际交付 FAILURE77 PUBLIC READY/REFROZEN；Root读取 public-harness-document-first-gate 的wholetype、owned verifier ESLint、Markdown221/356links/0missing、diff均0。新fixture和公开wrapper仅已落盘，等待脚本syntax门禁后主任务执行；既有成功公开合同不重复，77共享Route/Registry/Receipt/codec和描述符保持冻结。


## 夹具准备与首次公开失败的实际证据

首次 prepare 在 new-technical-identity-graph 因旧 source oracle 的 snapshot ID 并非已持久化 ID 退出1/5 setup；该事务全部 rollback。独立 correction 从 actual persisted Run 取 snapshot，实际完成96 setup、保留2个技术主体与12个新 Run/source，却在未先 claim 的 Run-bound grant 上409 `DREAM_THREAD_BINDING_CONFLICT` 退出1。原始文件/失败回执与已提交行均保留；continuation 不重新 seed，实际先 public claim→合法 grant→独立 public Run.fail，exit0/55 setup，原125完整行保留。两次错误均不冒充产品验收通过。

Luna首次公开 `run-failure77-public-contract.py` exit1，失败于 Exact safe failure code required。Root先执行 SELECT-only partial diagnose；第一次因沙箱阻止本机网络连接退出1、无数据库操作，网络权限下同一只读脚本 exit0/287 assertions。全部6个正例的 original SHA/version/result/entity scopes、一个 receipt/audit、完整原 recorder 的1 COMMIT/2 rollback/1 close、原 UPDATE raw与所有 source其他字段匹配；原125、prepublic125非目标完整行保留，17表 checkpoint已保存。

随后单个拒绝请求 editor diagnostic 实际 exit0/6 assertions：`403 DELEGATION_ENTITY_DENIED`，前后17全行字节相同，正例POST重放0。shared DelegationService.resolve 先比较 requiredRun 与 grant.runId，再检查 scope；Editor runId=null，原 fixture 预期 ACCESS_SCOPE_REQUIRED 与实际实体优先边界不符。保留原始失败与原 fixture，不改生产鉴权、不减弱断言。公开整体仍未通过。

续接责任由 Root/producer/Luna保持：producer新增独立 continuation harness，6正例只读恢复，16负例和27 GET重验，正例POST为零。Root另建独立新增 queued Run/source/PF/两次历史转换及必要的新 Deck/binding/snapshot（旧 fixture Deck disabled 保留），实际 public source.ensure→claim→Run-bound wrongRun grant，以及现存 ordinary Editor 的新 grant；不能改旧grant expiry/绝对上限、重置既有Run或重写已成功源。新600私有fixture仅修正已证明Editor expected code与更新短期OAuth/负例grant。准备完成后再次记录17完整检查点，新OAuth在harness READY后签发；原17全部行保留，尚未执行阶段不计通过。

## 续接 preflight 网络诊断与独立重试规划

公开 continuation 首次 exit1，safe label 为 preflight，没有完成恢复/负例/GET计数。Root分别执行 `python3 /private/tmp/ink-auth-migration-validation/failure77-continuation-preflight-diagnose.py sandbox` 与同文件 `network`：sandbox exit1、verification.target_role、loopback_permission_denied、零数据库断言；经授权网络权限 network exit0/23，真实目标/三角色/受限应用角色五项false/data_directory/17完整行anchor全部匹配。另一次纯 DTO/coverage/retained-row检查exit0；未据此宣称公开业务通过。原 continuation失败及原完整harness失败均保留。

### Optimized Prompt:

You are the primary coordinator continuing the frozen registered77 acceptance after an actual sandbox loopback denial. Existing immutable evidence proves all six positive writes committed exactly once, Editor binding-first403 is correct, strict fixture/evidence guards pass, and an elevated SELECT-only diagnostic passes23 for the exact owned51534 target, all three roles, least privileges and17 full-row anchor. Root owns credential preparation; the existing Luna owns one nondestructive public contract execution. Read the frozen continuation verifier, exact delegation DTO/handler and authoritative workflow context implementation. Preserve production77, the original harness, all failed receipts, every Run/source/history/grant expiry and six positive originals.

Because prior OAuth300s credentials have expired, create a new exclusive owned0600 fixture/evidence pair and a new signing JWK without exporting a private key. Reuse only the already owned independent queued negative Run/Thread and ordinary Editor entities to create two fresh grants through the existing public production creation Route. Do not seed another graph, reset an old Run, renew old expiries or replay source/claim/positive envelope writes. Confirm the previous17 full rows remain present and record the current17 full anchor. Keep every original case/receipt unchanged except the already proven Editor expectation and fresh credential labels. Sign browser OAuth300s last, then immediately delegate the exact frozen continuation through a new exclusive command wrapper with scoped loopback network access, safe output and no DDL/real-user/provider/filesystem work.

Accept only actual6 restored originals/16 denied POST/27 original GET/0 positivePOST with full17 preservation and entire source parity. Retain any new failure, diagnose its actual guard without weakening assertions or changing production state. Only after public0, update unexecuted primary atomic/preservation/scanner prerequisites to that exact accepted receipt and execute the already planned faults, final-COMMIT response loss, historical preservation and secret scan/archive/reference gate. Whole migration, real Google/account/model and all Dream no-PG runtime goals remain active.

USER REQUIREMENT:
继续跨项目重构；数据库接口遵从DTO/ORM；保护已同步worktree和实际已提交业务结果，保留失败回执，并完成公开与事务恢复验证。


## 公开续接、原子恢复与保全实际结果

首次 continuation wrapper 在 `preflight` exit1，没有执行业务案例；纯 DTO/coverage/retained guard exit0。Root只读诊断在默认sandbox因loopback permission denied exit1/0断言，同一脚本经限定网络权限exit0/23，真实target、三角色、五项受限角色标志、data_directory和17全行anchor全部匹配。原失败不覆盖。Root复用既有独立queued operand与普通Editor实体，只新建JWK和2个负例grant，实际准备exit0/8，旧图/Run/expiry与六个成功POST不重放；新OAuth300s最后签发。

Luna随后执行新独占network retry，实际exit0/556：六个已提交original只读恢复、16拒绝POST、27 Original GET、成功POST调用0、protected17；first public exit1、partial287和Editor probe6均作为独立历史保留。主任务随后三处具名fault实际exit0/126：source metadata UPDATE、receipt INSERT、audit INSERT分别503 `AUTH_SERVICE_UNAVAILABLE`且17表逐行回滚、原failed Run完整保留，自有trigger/function均清理。实际final-COMMIT执行后注入一次响应丢失，首次503，Original GET200、同输入replay200；仅一个metadata效果、一个receipt和一个audit，完整原recorder 1COMMIT/2rollback/1close与raw metadata/全部source字段一致。

最终SELECT-only preservation实际exit0/293：原125全行、prepublic125非目标全行、post-public17全行、六个positive original的owner/digest/scopes以及Run transitions/PF/bindings/history全部保留，自有fault function为0，正常数据库未触碰。第一次披露扫描exit1是把公开fixture的token selector标签和credential-source标签误当私密值；修订仍保留实际JWT/idg/DSN/password/private-key及编码扫描，最终107份0600来源、410个实际私密值、1499种编码和9类pattern对53个候选零命中。Failure77固定窗口由producer确认release；该结果不覆盖新failed transition、整条Runtime/Provider/FS或真实账户/模型验收。

- [failure77-public-continuation-network-retry-command-receipt.json](../exec/admin-auth-data-verification/failure77-public-continuation-network-retry-command-receipt.json)

- [failure77-atomic-recovery-command-receipt.json](../exec/admin-auth-data-verification/failure77-atomic-recovery-command-receipt.json)

- [failure77-post-validation-preservation-command-receipt.json](../exec/admin-auth-data-verification/failure77-post-validation-preservation-command-receipt.json)

- [failure77-thread-public-disclosure-final-proof.json](../exec/admin-auth-data-verification/failure77-thread-public-disclosure-final-proof.json)
