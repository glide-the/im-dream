# docs/design/claude-mcp — 目录合同

## 架构摘要

Claude MCP CLI 资源链接器的历史设计目录。当前业务与技术真相源已迁移到
`docs/design/claude-agent/dream-managed-mcp-resources.md`：数据库负责配置，标准 MCP
SDK 负责 discovery/OAuth，正常管理链路不调用 CLI。本目录只保留迁移前证据与独立
回滚背景，不得作为当前实现合同。

## 文件清单

| 文件 | 定位 | 职责 |
|---|---|---|
| `claude-mcp-resource-connector-design.md` | 已废止的 CLI 历史基线 | 迁移前调研证据、旧 API/OAuth 流程与独立回滚背景；当前实现禁止读取该 CLI 配置 |

## 同步记录

- 2026-08-25：目录降级为 CLI 历史基线；当前规范切换到 Dream-managed PostgreSQL/标准 MCP SDK 设计，OAuth callback 自动提交，正常管理链路零 CLI。
- 2026-08-25：资源链接器主设计同步匿名优先 HTTP 路由、正交 `auth_state`、用户主动 login、logout 后重探测，并链接新的认证路由重构设计。
- 2026-08-24：当前版本更新为正式 PyPI SDK `0.2.143` 版本/哈希锁与 MIT clean-room Runtime；记录 OAuth refresh token 跨进程 rotation、`invalid_grant` 清理、timeout late-write 防护，以及真实 Comfy 两轮 `get_server_info`、刷新 resume、Logout/Remove 回执。
- 2026-08-19: 修复 sequenceDiagram 消息文本中的 ASCII 分号歧义，并用项目 Mermaid 11 parser 验证全部 7 个时序图。
- 2026-08-20: 设计主文档完成 user-scoped 认证根、公开 SDK `mcp_servers` 定义交付、Linux `mcpOAuth` 最小投影、macOS secure-store reuse、每 turn 收敛、真实账户验收与 CLI 2.1.235 / SDK 0.2.140 原子发布门。
- 2026-08-19: 创建 Claude MCP 资源链接器设计目录与规范主文档。
- 2026-08-23: MCP management identity 与 Agent 共享自有 Runtime resolver/manifest 资格门；默认 Runtime 不合格时 fail closed，仅显式绝对 `CLAUDE_CODE_CLI_PATH` 可执行官方回滚。
- 2026-08-24：MCP public SDK dependency 切换到正式 PyPI `ink-claude-dream-agent-sdk==0.2.143`，以 archive SHA-256 锁安装并保留 `claude_agent_sdk` import/API；Docker 拒绝 official SDK 并存，Runtime 资格门保持不变。
- 2026-08-24：MCP 当前版本矩阵更新为自有 SDK `0.2.143`、production-qualified Runtime `main@7c34e6cd`/公开 npm `0.1.0`、Dream 接口资格标识 `2.1.241`、独立 Bun `1.4.0` 与 Docker official rollback `2.1.241`；记录五包 registry fresh install、`2.1.238`/`2.1.239` MCP 补丁和 `dmeck123@suoxya.com` 最终 Comfy OAuth/inventory/cleanup 回执。
