<!-- [Input] Existing Claude Agent Route Handler/import boundary and private BFF API owner. -->
<!-- [Output] Authenticated unbuffered HTTP/SSE forwarding and regression inventory. -->
<!-- [Pos] Explicit Claude streaming API folder contract. -->
<!-- [Sync] 2026-09-14: use shared BFF credentials and original transport extraction. -->

# Claude Agent proxy

| File | Pos | Function |
| --- | --- | --- |
| `[[...path]]/route.ts` | Public route | Thin GET/HEAD/POST/PUT/PATCH/DELETE/OPTIONS delegation. |
| `_proxy.ts` | Existing import boundary | Validates Claude API path and delegates shared BFF credential/transport logic. |
| `route.test.ts` | Stream regression | Test-owned loopback server checks first SSE flush before EOF, POST fidelity, exact server config and cleanup. |

No SSE parser/state machine is implemented here. Browser handle and native OAuth Bearer selection follow `../_auth/api-proxy.ts`; credentials never pass through response cookies/renewal headers. Local technical server lifetime is test-owned and does not touch user services.
