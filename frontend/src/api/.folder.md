# frontend/src/api — Folder Contract

## Architecture Summary
Frontend API client helpers for backend REST/SSE calls.
Keeps endpoint construction and protocol details out of feature components.

## File Table

| File | Pos | Function |
|---|---|---|
| `gatewayModelsApi.ts` | gateway-model-catalog transport node | Validate Admin Gateway model catalog/default alias and resolve an effective selection from an explicit saved preference or Admin's callable default. |
| `productApi.ts` | product-subscription transport node | Validate and transport the same-origin Dream Product BFF subscription, plan, usage, model, command, and payment contracts; mirror Admin's unavailable-plan state matrix without deriving subscription facts in the browser. |
| `resourceConnectorApi.ts` | `resource-connector api client node` | Provide fail-closed connector CRUD/auth/discovery/selection/sync-policy plus server-owned multi-Skill, real Hook/workspace operation, and safe-file helpers; raw provider data and filesystem paths stay excluded. |
| `voiceApi.ts` | voice-api client node | Provide session/deck/voice API helpers, lightweight Deck metadata input, default-Deck reconciliation, server policy facts, one shared Claude Agent SSE turn transport for Chat/Writing, Reflections analysis, and report storage; Writing has no random Voice selector. |
| `chatHistoryApi.ts` | Chat history transport | Share typed production create/list/delete calls between Chat, Deck Work, and unbound product-level Writing Threads; list supports actor-scoped `deck_id` filtering. |
| `claudeAgentSkillApi.ts` | common Skill catalog transport | Read the authenticated backend-owned common Skill slash command catalog without executing a Skill or duplicating package IDs. |
| `claudePluginAdminApi.ts` | ClaudePlugin administration transport | Expose the platform-global approved Marketplace catalog, exact entry-ID/manual install variants, shared operation/installation facts, uninstall, Deck refs, and plugin load receipts. |
| `claudeMcpApi.ts` | Claude MCP Resources transport | Expose managed-PostgreSQL capability and Server CRUD, cache-first tools/resources/prompts discovery, OAuth operation/redirect/cancel/logout, and safe error DTOs without CLI fallback or browser credential persistence. |
| `deckPluginApi.ts` | Deck runtime-version transport | Expose Agent type, exact binding/options/history DTOs, CAS saves, and validation; aggregate Deck versions remain separate. |
| `deckVersionApi.ts` | Deck content-version transport | Expose capability-backed draft state, no-write preview, explicit commit, immutable history, and structured CAS errors. |
| `storyWorkspaceApi.ts` | Story Workspace API client | Strictly parse Dream launch/files/re-entry contracts, including lifecycle-consistent initial/in-progress outcomes and canonical `run.href`. |
| `__tests__/` | API contract tests | Focused strict DTO and client projection tests; see `__tests__/.folder.md`. |

## Sync

- 2026-09-04: `claudeAgentSkillApi.ts` adds the authenticated common Skill slash-catalog read used by every Chat composer, including pre-Thread drafts.
- 2026-09-01: `voiceApi.ts` removes random Voice inspiration and exposes the existing shared Claude Agent SSE parser through `streamClaudeAgentTurn`; `chatHistoryApi.ts` permits product-level unbound Writing Thread creation with an optional title.
- 2026-08-31: `voiceApi.ts` removes daily-picture generation/save transports; Timeline retains authenticated historical picture reads only.
- 2026-08-31: `voiceApi.ts` routes incremental Writing inspiration and explicit historical-comment chat through the selected Voice's Claude Agent SSE Thread, retiring browser calls to `get_writing_suggestion`, `analyze_text`, and `chat_with_voice` PolyCLI transports.
- 2026-09-01: `resourceConnectorApi.ts` accepts capability schema v5's
  archive-backed `notion-diary-sync` and validates its unique `Read` + `Bash`
  tool combination without weakening allowed tool names.
- 2026-08-30: `resourceConnectorApi.ts` requires versioned capability installation metadata and preserves `requires_installation` separately from connection/scope availability.
- 2026-08-30: `resourceConnectorApi.ts` preserves both installed `notion-session` and `notion-cli` catalog entries, including the CLI package's truthful unavailable execution state.
- 2026-08-29: `resourceConnectorApi.ts` strictly validates real Read-hook/workspace-materializer descriptors, MCP integration state, parsed installed Skill body/reference descriptors, and revision-aware stable file reads.
- 2026-08-28: `resourceConnectorApi.ts` removes the obsolete localStorage/dummy connector path, distinguishes authenticated-without-snapshot from synced, preserves safe backend errors, and consumes the scheduled-sync policy DTO.
- 2026-08-29: `resourceConnectorApi.ts` drops opaque upstream `raw` data from discovery/selection and exposes a recoverable warning when a failed reauthorization leaves the effective credential usable.
- 2026-08-25: `claudeMcpApi.ts` requests cache-first automatic detail inventory (`force: false`) so revisits reuse valid snapshots instead of forcing remote discovery.
- 2026-08-25: Claude MCP Create/PATCH transport 不再发送 `auth_kind`；认证分类完全消费后端 discovery 状态。
- 2026-08-25: `claudeMcpApi.ts` switches the Resources boundary to managed-DB Server CRUD and explicit standard-MCP tools/resources/prompts discovery; list reads never trigger remote discovery or Claude CLI management commands.
- 2026-08-25: `claudeMcpApi.ts` exposes revision-aware managed Server PATCH so the detail page can edit transport/endpoint/profile/enabled state without CLI fallback.
- 2026-08-25: `claudeMcpApi.ts` carries backend/Runtime-classified anonymous, required, authenticated, and rollback-compatible unknown authentication state.
- 2026-08-21: `claudeMcpApi.ts` carries backend-authoritative config scope/removability and accepts absolute HTTP(S) server configuration.
- 2026-08-20: `claudeMcpApi.ts` adds the sanitized, prompt-free server inventory DTO including formal tool safety annotations and explicit not-reported resource/prompt capabilities.
- 2026-08-19: `claudeMcpApi.ts` adds the typed, backend-only transport for the Claude MCP Resources connector.
- 2026-08-19: `claudePluginAdminApi.ts` adds the global catalog DTO with immutable ref/commit/manifest/full-plugin digest lineage and sends only `marketplace_entry_id` for Marketplace installs.
- 2026-08-13: `productApi.ts` mirrors the Admin/Dream unavailable-plan contract, including published versions that remain unavailable because required configuration is incomplete.
- 2026-08-14: `gatewayModelsApi.ts` centralizes new-user effective model selection: preserve an explicit saved alias, otherwise use only Admin's validated callable `defaultModelAlias`.
- 2026-08-14: `voiceApi.ts` adds the authenticated `POST /api/decks/defaults/reconcile` client used before the first Deck list read.
- 2026-08-14: Deck DTOs consume the server-derived Agent type/revision used by Chat runtime selection.
- 2026-08-14: `storyWorkspaceApi.ts` rejects Dream outcome values or outcome/lifecycle combinations outside the initial/in-progress contract.
- 2026-08-15: `voiceApi.ts` recognizes `default_created` when the existing reconciliation endpoint provisions a missing default for a legacy actor.
- 2026-08-16: `deckVersionApi.ts` separates Deck content vN from runtime plugin semver and carries draft/base CAS for preview/commit.
- 2026-08-17: `chatHistoryApi.ts` centralizes Chat history transport and supplies Deck-filtered previews/deletion to Settings / Work.
- 2026-08-17: `voiceApi.ts` carries server `can_publish` / `publish_block_reason` only to classify system-initialized defaults; it still exposes no active publish transport.
- 2026-06-06: created folder contract.
- 2026-06-06: remove `initializeMemoryWorkspace` and Voice scenario memory-init calls. Add `analyzeReflectionsSection` flow: create thread → reflections/memory-init → claude-agent SSE → GET messages → parse text parts.
- 2026-06-12: `voiceApi.ts` consumes `frontend/src/lib/apiBase.ts` instead of hard-coding `/ink-and-memory`, enabling cross-origin API requests in deployed frontend.
- 2026-06-12: remove obsolete inline Reflections prompt/agent helpers now superseded by `analyzeReflectionsSection`.
- 2026-07-04: `resourceConnectorApi.ts` added the Notion resource connector client and fallback storage wiring for connector create/auth/resource flows.
- 2026-07-04: `resourceConnectorApi.ts` now unwraps backend `{ connector }` create responses before local fallback so dashboard auth/discovery/select/sync flows keep the real UUID connector id instead of a local `connector_*` id.
- 2026-07-04: `resourceConnectorApi.ts` now treats connector `pending` as `draft` until a verification session exists, so the dashboard can surface `连接 Notion` before the auth login flow is started.
- 2026-07-05: `resourceConnectorApi.ts` now sends resource selection using the backend's `selected_databases` / `selected_pages` payload names so the persisted connector selection survives the `/resources/select` round-trip instead of being dropped as empty arrays.
- 2026-07-05: `resourceConnectorApi.ts` now normalizes backend auth sessions (`auth_session_*`) so `consumed`/`failed` auth states become explicit frontend errors with messages, preventing indefinite `authenticating` polling loops.
- 2026-07-05: `resourceConnectorApi.ts` now prioritizes `auth_session` terminal status from backend connector payloads for auth and connector state calculation, then falls back to top-level auth status.
- 2026-07-07: `resourceConnectorApi.ts` now prefers connector `auth_status` / normalized auth session status ahead of stale top-level `status`, preventing consumed sessions from regressing an already authenticated connector in the UI.
- 2026-07-07: `resourceConnectorApi.ts` exports the connector normalizer for UI-only fallback workbench data so embedded connector fallback uses the same client shape as backend and localStorage responses.
- 2026-07-08: `resourceConnectorApi.ts` local fallback creation now replaces existing same-platform connectors so offline/local state follows the one-authenticated-account-per-platform rule.
- 2026-07-08: `resourceConnectorApi.ts` posts full selected Notion resource objects to `/resources/select` and normalizes backend `connector_resources.external_id` as the frontend source id so persisted selections survive refresh and feed Chat linked-resource summaries.
- 2026-07-09: `resourceConnectorApi.ts` exposes a browser-local connector changed event so Settings resource saves can prompt Chat connector summaries to reload.
