# backend/services/deck — Folder Contract

## Architecture Summary

Deck-domain application services. This folder resolves server-owned Deck
plugin policy, runtime locks/context, default provisioning, and Story Workflow
operations without accepting filesystem paths or installation identities from
the browser.

## File Table

| File | Pos | Function |
|---|---|---|
| `__init__.py` | package init | Mark the Deck service package. |
| `admin_gateway.py` | Deck Plugin admin adapter | Materialize server-published runtime locks for Deck Plugin administration. |
| `agent_type.py` | Deck Agent type policy | Derive `chat` / `dream` from active published Deck Plugin capabilities and expose list-safe decoration. |
| `builtin_plugin.py` | built-in plugin source | Seed and resolve the repository-owned Dream Story plugin. |
| `chat_context.py` | chat Deck context | Validate persisted Deck Claude-plugin refs and expose provenance to Chat. |
| `defaults.py` | Deck-default policy service | Resolve the configured verified Claude plugin, provision new screenplay defaults, create missing legacy-account defaults, and preserve explicit refs while repairing empty ones. |
| `runtime_context.py` | runtime compatibility adapter | Resolve server-owned Deck Plugin runtime compatibility facts. |
| `sharing.py` | Deck sharing policy | Derive publication eligibility and reject system-default publishing, self-collection, and collection of unpublished Decks. |
| `story_workflow_application.py` | Story Workflow application service | Implement focused Dream workflow business operations. |

## Sync

- 2026-08-14: Added the folder contract and `defaults.py`; default plugin resolution is shared by new Deck creation/provisioning and explicit non-destructive legacy default reconciliation.
- 2026-08-14: Added `agent_type.py`; Deck list/detail responses derive Agent type from active published binding facts, while unbound or invalid legacy data fails closed to Chat.
- 2026-08-14: Added `sharing.py`; Deck publication and collection rules now share one server-owned policy instead of relying on UI-only hiding.
- 2026-08-15: `defaults.py` keeps the existing reconciliation boundary but now ensures legacy actors with other Decks also receive the configured screenplay default.
