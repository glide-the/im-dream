# MCP Apps Phase 3 backend tests

Provider-free governance tests for strict policy parsing and production-off
state. Node package tests own plugin lifecycle, limits, concurrency, and redaction.

| File | Function |
|---|---|
| `test_policy_governance.py` | Proves version/revision/state validation, canonical colon-bearing Server keys/path rejection, finite positive view TTL, and production-off projection. |

## Sync

- 2026-09-06: added policy compatibility, invalid-state, path, and non-finite TTL fail-closed tests.
