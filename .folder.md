# Ink & Memory — 项目根目录

## 架构概述
Monorepo：`frontend/`（React/Vite SPA）+ `backend/`（FastAPI/Python）前后端分离，通过 nginx 反向代理整合。

## 文件清单

| File | Pos | Function |
|------|-----|----------|
| CLAUDE.md | root/控制平面 | Agent 工作区控制平面，规则指针 |
| AGENTS.md | root/Agent 契约 | Agent 工作契约与治理规范 |
| README.md | root/使用入口 | 面向贡献者和本地用户说明功能、分支状态、Admin/Gateway/embedded PostgreSQL 启动方法、页面入口与功能 TODO |
| .vscode/settings.json | root/IDE 配置 | 固定 VS Code Python 默认解释器为 `backend/.venv/bin/python`，并在新终端中自动激活该后端环境 |
| docker-compose.yml | root/编排 | 本地 Docker 编排（Mihomo `tun-proxy` + 前端 nginx + 后端 uvicorn）；backend 共享 `tun-proxy` 网络命名空间以路由出站；向前端根路径构建和后端 SEO 生成器传递本地公开 app/API URL；backend 容器带 Claude Code bubblewrap Bash sandbox 所需 Docker runtime 权限 |
| deploy/ | 部署脚本集 | 平台化发布脚本入口：local、docker、remote-ssh、google-cloud；阿里云使用 remote-ssh，Dream 只发布 frontend/backend，Admin 数据平台归相邻仓库 |
| .storage-env | root/配置（gitignored） | setup-storage.sh 生成的桶名和 SA 邮箱 |
| .cloud-env | root/配置（gitignored） | setup-env.sh 生成的 Cloud Run 环境变量和 Secret 引用 |
| frontend/ | 前端服务 | React 18 + Vite SPA，nginx 容器化服务 |
| backend/ | 后端服务 | FastAPI + uvicorn，Python 容器化服务 |
| docs/ | 文档 | 架构/设计/规范文档 |
| assets/ | 静态资源 | 项目静态资源 |
| scripts/ | 仓库验证与运维脚本 | `run-dream-business-e2e.mjs` 使用自有临时 PostgreSQL/端口/本地 Provider 验证 Admin + Dream + 浏览器业务链；`INK_REGISTRATION_DEFAULTS_ONLY=1` 选择当前注册→Free→默认模型→首次调用车道，其中默认模型仅预置 Provider/Pricing、不预置 Entitlement，以覆盖真实自举失败；`verify_claude_registry_release.py` 在 SDK/Runtime 真正上架后以临时 venv/npm project 验证 PyPI/npm 精确制品、五包、零 source map、两个 CLI alias 与 SDK/API 合同，不调用模型 |

## Sync

- 2026-08-26: AutoDL Dream now projects and initializes Thread workspace, shared Artifact, local files, service home, and Claude plugin runtime under `/root/autodl-tmp/ink-memory`; Admin PostgreSQL remains outside this Dream resource root.
- 2026-08-26: VS Code workspace Python discovery now resolves to `backend/.venv/bin/python`; only the shared interpreter setting is admitted from the otherwise ignored `.vscode/` directory.
- 2026-08-24: Added a provider-free Claude SDK/Runtime registry release acceptance harness; it fails closed on missing releases and never changes dependency locks.
- 2026-08-26: Updated the registry acceptance harness for Runtime 0.1.1 standalone manifests, both CLI aliases, shared business receipt, capability/checksum evidence, and selector-to-platform executable binding; legacy 0.1.0 validation remains available for rollback artifacts.
- 2026-08-22: Local Dream startup follows the Admin-owned embedded PostgreSQL env file; Dream does not supervise or migrate the database.
- 2026-08-17: Root README is now usage-first, records develop/story-workspace/Deck branch integration, and moves unfinished capabilities into one bottom TODO list.
- 2026-08-14: 隔离全栈 business E2E 先从无既有 Entitlement 的 priced/enabled 模型发布 Admin-owned Free plan，再从浏览器真实注册新用户，验证 Product/Settings/首次 Agent/Gateway Token 结算，结束时清理自有容器、进程和临时文件。
- 2026-06-14: `docker-compose.yml` adds `FRONTEND_PUBLIC_SITE_URL` / `BACKEND_PUBLIC_SITE_URL`-driven local SEO URL propagation for frontend static metadata and backend robots/sitemap/llms generators.
- 2026-06-15: Local Compose frontend public URL default changed from `/ink-and-memory/` to `/`.
- 2026-06-17: Local Compose backend now grants `SYS_ADMIN` and unconfined seccomp/AppArmor for Claude Code bubblewrap Bash sandbox, and persists `AGENT_CWD` under `/app/data`.
- 2026-06-23: Local Compose now includes Mihomo `tun-proxy`; backend shares the proxy network namespace and publishes backend port through the proxy container.
- 2026-08-21: Alibaba Cloud deployment uses separate Admin-owned data-platform and Dream-only application releases connected by an explicit external Docker network.
