# docs/deploy/

## 架构概述
发布体系文档入口，区分本地直跑、Docker 容器、Remote SSH、AutoDL 直宿主和 Google Cloud Run 拓扑；阿里云 ECS 属于 Remote SSH 配置。当前 frontend 为 Next.js 16 standalone + pnpm frozen lock；AutoDL 已迁移，Google Cloud 数据路径仍未迁移。

> [Sync] 2026-09-06：AutoDL 直宿主已迁移到 standalone Next.js、frozen pnpm 和 Node MCP Apps Runtime；Google Cloud/Remote 数据脚本仍保留 SQLite，Compose/Cloud 的 `VITE_PUBLIC_SITE_URL` build arg 仍未被 Dockerfile 消费。
> [Sync] 2026-09-12：新增显式 NATAPP origin 的阿里云边缘转发切换、准确备份、验证和回滚合同；不启停上游服务或修改数据。
> [Sync] 2026-09-13：Dream 采用 SDK 0.2.145 × 唯一原始 src Runtime 0.1.9，backend/frontend 版本为 0.1.3/0.0.3；新增 CI 发布与本机接入设计/证据状态，0.1.4 仅为历史回执。
> [Sync] 2026-09-13：0.1.9 接入记录补充用户确认的真实模型/Notion E2E，以及后端按要求停止的后续状态；不重启服务，Docker 构建仍未验收。

> [Sync] 2026-08-21: 新增阿里云双仓库发布合同；Admin 仓库拥有
> Admin/Gateway/内嵌 PostgreSQL/migration，Dream 仓库只发布 frontend/backend；MinIO 暂停。
> [Sync] 2026-08-22: 阿里云 Dream 的 Gateway/Product API 共用 Admin HTTPS
> origin；共享网络 alias 仅用于 embedded PostgreSQL。

> [Sync] 2026-08-26：同步自有 Claude SDK/Runtime 中文打包、发布与 Dream 集成手册；
> 历史回执：SDK `0.2.144` 已发布到 PyPI，当时 Runtime `0.1.1` 五包已发布到 npm，Dream 使用
> 精确版本/archive 哈希和 fail-closed Runtime resolver。
> [Sync] 2026-08-28：Runtime 操作手册与 registry 验收采用公开 `0.1.2`，绑定 qualification
> `33149053281`、publish `33151128000`、provider-free fresh install 和按用户要求保留的五包最小权限 Secret。
> [Sync] 2026-08-28：当前 Runtime 操作手册与 registry 验收推进到公开 `0.1.3`，绑定 qualification
> `33157330350`、publish `33157476036`、provider-free fresh install、Dream 默认 resolver 和保留的五包最小权限 Secret；上一行作为 `0.1.2` 历史回执保留。
> [Sync] 2026-08-30：当前 Runtime 操作手册与 registry 验收推进到公开 `0.1.4`，绑定 qualification
> `33306855166`、publish `33306940462`、Notion Bash capability、provider-free fresh install 和 Dream Docker/README/resolver 原子版本面；上一行作为 `0.1.3` 历史回执保留。
> [Sync] 2026-08-31：部署拓扑与排障说明移除已退役的 `/polycli` 代理；同源 fallback 仅保留当前 API/Auth/OAuth 边界。
> [Sync] 2026-09-04：发布后 Dream smoke 增加 committed reply、typed
> post-Hook sync terminal 与 Execution `dream-files` 刷新检查；本修复不含 migration、配置或部署动作。

> [Sync] 2026-08-22: 本地 Dream 通过可配置 Admin env 文件消费 embedded PostgreSQL
> 连接，禁止继续复制已经失效的旧 Docker PostgreSQL DSN。

> [Sync] 2026-08-19: 后端运行基线统一为 Python 3.12、官方 Node 22.18.0、Agent SDK 0.2.140 与 Claude Code 2.1.235；镜像构建验证 exact Node/CLI 和正式 MCP argv，nested sandbox 使用公开 optional-seccomp skip，不再安装或修改 vendor sandbox-runtime。
> [Sync] 2026-08-22: Remote SSH 文档新增低内存 Claude Agent 单进程准入、
> legacy memory MCP default-off、只读 RSS/cgroup 采样和部署后真实验收合同。

> [Sync] 2026-08-22: 阿里云真实 resume turn 证实 backend 读取风暴可令系统盘
> iowait 达 69.85%；部署文档新增可配置 block-device read BPS/IOPS 限制、回滚与验收回执。

> [Sync] 2026-06-14: Remote SSH Docker 后端镜像和 Compose 文档补充
> Claude Code Bash sandbox 依赖、自动 nested sandbox 检测与
> `seccomp=unconfined` 运行约束。
> [Sync] 2026-06-17: Docker Compose 与 Remote SSH Compose 补充 backend
> `SYS_ADMIN` 和 `apparmor=unconfined`，解决 bubblewrap
> `Failed to make / slave`。
> [Sync] 2026-06-17: Docker/Remote SSH 排障补充 `bwrap` 在
> `/newroot/sbin` 挂载 tmpfs 失败；backend 镜像需包含标准 `sbin`
> 目录，workspace sandbox allowlist 需放行已存在的 `sbin` 运行时路径。
>
> [Sync] 2026-06-15: 发布验收 URL 统一为前端根路径 `/`，
> SEO 静态入口为 `/robots.txt`、`/sitemap.xml`、`/llms.txt`。
>
> [Sync] 2026-06-16: Remote SSH 数据维护收敛为
> `backup` / `upload` / `download`，上传后 force-recreate Compose 服务加载数据库。
>
> [Sync] 2026-06-23: 发布文档补充生产 Google OAuth public URL、
> Secure/SameSite cookie、credentials CORS 与 Secret Manager 配置要求。
>
> [Sync] 2026-06-23: Docker/Remote SSH 文档补充 Mihomo TUN
> `tun-proxy`，backend 共享 proxy 网络命名空间，真实 Clash 配置保持
> gitignored。
>
> [Sync] 2026-06-25: Remote SSH sandbox 排障补充 `HTTP 403` /
> `blocked-by-allowlist` 分支，优先按 sandbox network allowlist policy
> deny 处理；无该 header 的 reset/502/timeout 再走子进程代理出口排障。

## 文件清单

| File | Pos | Function |
|------|-----|----------|
| README.md | 发布文档入口 | 分流当前可执行路径与阻塞路径，记录 Next/pnpm、Admin-owned PostgreSQL、配置漂移和验收边界 |
| overview.md | Cloud Run 历史合同与阻塞记录 | 前端可构建 Next standalone，但旧 SQLite/GCS 数据路径和 ignored VITE build arg 尚未迁移；不是当前生产手册 |
| data-sync.md | 历史云数据同步记录 | 保留 SQLite/GCS 脚本行为与风险；不得作为当前 Dream 业务数据同步或发布步骤 |
| remote-ssh.md | Remote SSH 部署 | SSH/rsync/Compose 构建当前 Next standalone；旧 SQLite 数据维护命令只作历史识别，真实数据服从 Admin PostgreSQL/Drizzle |
| natapp-edge-relay.md | NATAPP 边缘转发 | 以两个显式 origin 更新 Dream/Admin nginx 站点，保留 SHA-256 备份、语法门禁、平滑 reload、SSE/WebSocket 与准确回滚 |
| aliyun.md | 阿里云 ECS 部署 | 双仓库 Compose 与 edge-only NATAPP relay 两种显式拓扑；Admin-owned 数据/migration、Dream-only 应用、首次引导、共享网络、I/O budget、验证与回滚边界 |
| autodl.md | AutoDL direct-host 部署 | Next.js/pnpm + Node MCP Apps + FastAPI 的直宿主发布、验证与回滚合同 |
| release-system-design.md | 历史发布体系设计稿 | 保留旧 Vite/npm/nginx/SQLite/GCS 方案，不作为当前操作手册 |
| claude-sdk-runtime-packaging-and-integration.md | Claude SDK/Runtime 发布操作手册 | Python SDK 可复现 wheel/sdist、TestPyPI/PyPI OIDC、Bun 四平台 npm 五包、Dream 精确版本/哈希接入、测试和回滚 |
| claude-registry-release-acceptance.md | Claude registry 发布验收 | SDK/Runtime 上架后的 PyPI/npm 精确制品、standalone 安装与 fail-closed provider-free 门禁 |
| runtime-0.1.7-source-structure-integration.md | 已被取代的历史说明 | 仅恢复旁路快照，未统一实际实现；由 0.1.9 修正 |
| runtime-0.1.8-source-structure-integration.md | 历史源码接入说明 | 重复源树阶段，已由 0.1.9 去歧义/发布/采用方案取代 |
| runtime-0.1.9-release-and-local-dream-adoption.md | 当前发布与本机接入 | 唯一原始 src、CI 五包发布、精确 pin/项目版本、正常 PATH 安装/受控进程验证与回退 |
| ../../deploy/google-cloud/sync-data.sh | 未迁移的 Google Cloud 同步脚本 | 仍上传 SQLite 到 GCS；与 Admin-owned PostgreSQL 合同冲突，只能作为迁移输入 |
