# frontend/packages

## Architecture

Sibling Node packages owned by the canonical `frontend/` pnpm workspace. Packages
must expose explicit runtime boundaries and may not create nested lockfiles or a
second Next.js project.

## Files

| Path | Function |
|---|---|
| `mcp-apps-runtime/` | Server-only Node MCP Runtime consumed only by the root App Router's thin MCP Apps route. |

## Sync

- 2026-09-05: registered the canonical sibling MCP Apps Runtime package without changing the 411-03-owned lock.
