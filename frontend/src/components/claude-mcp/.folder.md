# frontend/src/components/claude-mcp — Folder Contract

## Architecture Summary

Owns the Settings Resources presentation for the `claude-mcp` business domain. Components consume only the typed Claude MCP API, retain redirect URLs in transient component state, recover live operations from backend IDs, and never persist OAuth credentials or emulate CLI state locally.

## File Table

| File | Pos | Function |
|---|---|---|
| `ClaudeMcpResourceSection.tsx` | Resources feature surface | Render capability gating, discovered servers, browser authorization handoff, redirect submission, progress recovery, cancel/retry, and logout. |

## Sync

- 2026-08-19: Created the minimal Claude MCP Resources interaction using shared Settings design tokens.
