# deploy/autodl-ssh/runtime — Folder Contract

## Architecture Summary

Direct-host runtime scripts retained from the historical AutoDL release shape.
Persistent-root safety remains relevant, but the frontend supervisor still
starts Vite Preview and is incompatible with the current Next.js application.

## File Table

| File | Pos | Function |
|---|---|---|
| `init-dream-data.sh` | persistent-data initializer | Creates and validates Dream agentdata, actor-scoped Notion runtime, artifact, upload, plugin, and log roots with private ownership/modes and symlink rejection. |
| `start-dream.sh` | blocked historical supervisor | Starts Vite Preview and FastAPI; it cannot start the current standalone Next.js frontend and must not be used to qualify revision `54f3bbe5`. |
| `start-ink-memory.sh` | historical release restart entry | Restores only the old release shape and inherits its invalid `dist/index.html` checks; process-ownership safeguards remain requirements for a future migration. |

## Sync

- 2026-09-06: mark the Vite supervisor and release restarter incompatible with the canonical Next.js/pnpm frontend while preserving persistent-root and process-ownership requirements.
- 2026-08-28: direct-host startup binds `INK_NOTION_RUNTIME_ROOT` to persistent agentdata so actor credential homes and canonical snapshots never fall back to the process user's home.
