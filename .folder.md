<!-- [Input] Root repository structure, operator docs, deployment status, and source/configuration at commit 54f3bbe5. -->
<!-- [Output] Root ownership and inventory contract for repository maintainers. -->
<!-- [Pos] Root folder contract; detailed operator behavior belongs in the mirrored READMEs and deploy/README.md. -->
<!-- [Sync] 2026-09-06: align root inventory with the sole frontend Next/pnpm workspace and explicit deployment gaps. -->
<!-- [Sync] 2026-09-12: index the explicit-origin, recoverable NATAPP edge-relay deployment path. -->
<!-- [Sync] 2026-09-13: index the SDK 0.2.145 and package-root Runtime 0.1.7 source contract. -->

# Ink & Memory — 项目根目录

## 架构概述

本仓库只拥有 Dream 应用：`frontend/` 是 React 19 + Next.js 16 的唯一 Node workspace/Web package，`backend/` 是 FastAPI/Python 服务。Next Route Handler 提供同源 Web 边界和 server-only MCP Apps adapter；Python 继续拥有业务身份、数据能力和 managed-MCP 配置。Admin/Gateway/PostgreSQL 及共享 Schema 由相邻 `ink-admin-memory` 仓库所有。

提交 `54f3bbe5` 已完成前端源码和构建 owner 收敛，但不能据此声明生产发布：MCP Apps 的公开 `productionAppsEffective` 仍固定为 `false`，部分部署/CI 脚本仍绑定已删除的 npm/Vite 输入。当前部署状态统一以 `deploy/README.md` 为准。

## 文件清单

| File | Pos | Function |
| --- | --- | --- |
| `AGENTS.md` | 根工作合同 | Schema、单一运行路径、真实业务测试、工作区安全与通用产品设计规则 |
| `Agent.md` | 根维护合同 | README 中英文同源、SDK/Runtime 原子版本配对、包管理器、PATH、Secret、Schema 与验证规则 |
| `README.md` | 英文操作入口 | Canonical operator guide；说明所有权、精确版本、安装、Next/pnpm 运行、验证、部署缺口与故障排查 |
| `README.zh.md` | 中文操作入口 | 与 `README.md` 同结构、同事实、同命令的中文镜像 |
| `frontend/` | Web/Node 服务 | 唯一 Next.js 16 + React 19 + pnpm workspace；App Router 位于 `app/`，Dream 浏览器源码位于 `app/_dream/`，独立 Node Runtime 位于 `packages/mcp-apps-runtime/` |
| `backend/` | Dream 后端 | FastAPI、业务 API、认证、Agent/Thread/Run、managed MCP 配置与 SDK/Runtime composition；只依赖 Admin 已发布 capability |
| `deploy/` | 部署脚本与状态 | 平台入口、已知阻断和安全边界；AutoDL 使用当前 Next/pnpm direct-host，阿里云可显式选择 Compose 或可恢复的 NATAPP edge-only relay，其他容器路径仍有配置/验收缺口 |
| `docker-compose.yml` | 本地容器编排 | 构建当前 frontend Next Dockerfile、FastAPI 与 Mihomo；仍传递被 Dockerfile 忽略的 `VITE_PUBLIC_SITE_URL`，不能作为 SEO/public-origin 注入证据 |
| `.github/workflows/` | 自动化 | 后端工作流之外，现有前端 CI/静态发布仍假定已删除的 npm lock/`dist` 产物；迁移前不能资格化当前 Next 源码 |
| `docs/` | 设计与规则 | 架构、产品、执行证据、规则与部署说明；历史回执不得替代当前源码或生产验收 |
| `scripts/` | 验证与运维 | 包括 provider-free SDK/Runtime registry acceptance 和明确隔离的业务 E2E harness；技术 fixture 不得汇报为真实业务验收 |
| `assets/` | 静态资源 | README 与项目公共静态资产 |
| `.vscode/settings.json` | IDE 配置 | 默认使用 `backend/.venv/bin/python`，并在新终端自动激活后端环境；集中配置本机前后端调试地址与端口 |
| `.storage-env`, `.cloud-env` | gitignored 配置 | 部署 setup 脚本生成的私有环境引用，不得提交 Secret |

## Sync

- 2026-09-13: Dream project metadata receives backend 0.1.1/frontend 0.0.1 patch bumps and the exact Runtime 0.1.7 contract; the upstream research tree is restored in the separate Runtime repo, not copied into Dream or deployed.

- 2026-09-13: Dream pins SDK 0.2.145 and the Runtime 0.1.7 package-root `cli.js` source contract; the unpublished Runtime stays fail closed until registry evidence exists.
- 2026-09-12: 根目录索引新增显式 NATAPP origins 的阿里云边缘代理切换，同时保留应用、数据库与上游实例所有权边界。
- 2026-09-06: 根文档与提交 `54f3bbe5` 的 Next.js 16、React 19、pnpm 10.28.1、`frontend/app/_dream` 唯一源码 owner 和 `frontend/packages/mcp-apps-runtime/src` server-only owner 对齐。
- 2026-09-06: 部署状态改为事实矩阵：local Next/pnpm 当前可验证；root/remote/Google Cloud 容器路径仍有 ignored Vite 参数或资源假设；AutoDL 与旧 frontend workflows 当前不兼容；所有生产声明仍需独立验收。
