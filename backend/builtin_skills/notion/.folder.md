# backend/builtin_skills/notion — Folder Contract

## Architecture Summary

Notion platform builtin Skill namespace. The current actor/thread selects this
namespace only after its existing Notion credential projection is effective;
directory packages link read-only and `.skill` packages unpack into that thread.

## File Table

| Path | Pos | Function |
|---|---|---|
| `notion-session/` | directory built-in Skill | Read-only lightweight-index navigation and Runtime page Read-hook guidance. |
| `notion-cli/` | directory built-in Skill | Direct `ntn` CLI workflows over the current actor/thread credential projection. |
| `notion-diary-sync.skill` | archive built-in Skill | Canonical Skill ZIP unpacked flat into an enabled thread; reads current EditorState and writes only to a selected Notion data source on explicit user intent. |

## Sync

- 2026-09-01: group all three Notion packages under one platform namespace,
  add canonical `.skill` extraction, and remove fixed business IDs and temporary
  environment workarounds from the diary synchronization package.
- 2026-09-04: `notion-cli` emits only the exact read-only `ntn` forms covered
  by Dream's actor/thread-bound Auto policy; the Skill never grants Bash itself.
