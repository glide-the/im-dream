# docs/design/claude-mcp — Folder Contract

## Architecture Summary
Claude MCP 资源链接器的业务与技术设计真相源。
只定义公开 Claude Code CLI argv、Resources 交互、身份隔离、安全和发布门，不复用私有 SDK control subtype。

## File Table

| File | Pos | Function |
|---|---|---|
| `claude-mcp-resource-connector-design.md` | claude-mcp canonical design | 调研证据、业务边界、API/DTO、OAuth 进程状态机、Mermaid 时序、正式评审与最小实现门 |

## Sync

- 2026-08-19: 修复 sequenceDiagram 消息文本中的 ASCII 分号歧义，并用项目 Mermaid 11 parser 验证全部 7 个时序图。
- 2026-08-20: 设计主文档完成 user-scoped 认证根、公开 SDK `mcp_servers` 定义交付、Linux `mcpOAuth` 最小投影、macOS secure-store reuse、每 turn 收敛、真实账户验收与 CLI 2.1.235 / SDK 0.2.140 原子发布门。
- 2026-08-19: 创建 Claude MCP 资源链接器设计目录与规范主文档。
- 2026-08-23: MCP management identity 与 Agent 共享自有 Runtime resolver/manifest 资格门；默认 Runtime 不合格时 fail closed，仅显式绝对 `CLAUDE_CODE_CLI_PATH` 可执行官方回滚。
- 2026-08-24: MCP public SDK dependency切换到 `ink-claude-dream-agent-sdk==0.2.143` 的不可变 Git commit，保留 `claude_agent_sdk` import/API；Docker 拒绝 official SDK 并存，Runtime 资格门保持不变。
- 2026-08-24: MCP 当前版本矩阵将 Docker official rollback CLI 更新为 `2.1.241`，与自有 SDK 的 CLI pin 原子对齐并由 build/static contract 双重断言；默认自有 Runtime 资格门不变。
