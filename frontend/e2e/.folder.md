# frontend/e2e — Folder Contract

## Architecture Summary

Playwright business-journey specifications for the production frontend entry
points. Provider-free tests use production-shaped API fixtures and strict
unexpected-request/browser-diagnostic audits; explicitly named real tests use
their documented PostgreSQL, Gateway, and model harnesses.

## File Table

| File | Pos | Function |
|---|---|---|
| `writing-manual-suggestions.spec.ts` | provider-free Writing suggestion journey | Mount real EditorEngine/controller/Cell components with deterministic dependencies and verify manual insertion, incremental completion, latest-only same-Cell Refresh, one Session Thread, snapshot binding, light desktop, dark Chinese mobile, touch sizing, overflow, and diagnostics. |
| `story-workspace-default-entry.spec.ts` | authenticated-entry journey | Prove root password login and unsupported `/ink-and-memory/` entry resolve to canonical Story Workspace Chat, then verify Writing opens its related canonical Thread Chat in a draggable right pane. |
| `chat-first-deck-defaults.spec.ts` | Deck home/Work/version journey | Prove user published-clean filtering, default-visible system Decks, shared Deck preview, typed Chat editable prefill/Dream workbench launch, Work inventory, related Chat preview/deletion, version maintenance, responsiveness, exclusions, and diagnostics. |
| `story-workspace-real-deck-home.spec.ts` | Read-only real Deck/Work acceptance | Compare real Deck eligibility, user/System preview, real example-to-Chat prefill, full Work inventory, Deck-filtered related Chat previews, and zero mutation requests. |
| `story-workspace-real-default-deck-plugin.spec.ts` | opt-in real default-account journey | Use the existing local account and normal services to verify responsive Deck UI and the configured server-owned plugin facts without shadow data. |
| `deck-dream.spec.ts` | Deck-to-Dream journey | Exercise Deck selection and Dream handoff behavior. |
| `claude-plugin-settings.spec.ts` | ClaudePlugin Work technical journey | Use production-shaped intercepted API fixtures to verify menu keyboard/focus behavior, Comfy selection/revision confirmation, exact entry-ID install polling/result, capability failure recovery, and narrow viewport placement without real business writes. |
| `claude-mcp-resources.spec.ts` | Claude MCP Resources technical journey | Verify the automatic browser-only secret-safe OAuth callback plus HTTP(S) configuration, backend-derived auth state/removability, OAuth cancel/retry, cache-first automatic server inventory, tool filters, logout/remove, refresh and narrow layout with intercepted production DTOs. |
| `notion-connector-settings.spec.ts` | Notion Settings technical journey | Verify disabled Feishu/local CLI placeholders, compact two-column desktop/single-column mobile detail without redundant notices, truthful non-MCP capability state, deferred resource discovery, Skill/file/resource/source child views, links, responsiveness, dialog-free disconnect, and isolated-port Vite HMR diagnostic separation. |
| `notion-cli-runtime-real.spec.ts` | opt-in real Notion CLI journey | Use the named existing actor and visible Settings/Chat path to prove exact safe set/unset receipts, read-only `ntn` version/doctor/identity, same-thread resume refresh, and an ordinary no-tool turn without changing the connector. |
| `claude-mcp-resources-real.spec.ts` | 可选真实账户 MCP 流程 | 通过可见 Resources 验证 managed-DB Streamable HTTP、legacy SSE、server-owned stdio、后端判定匿名/OAuth、同源自动 callback、自动 tools/resources/prompts inventory、Chat 两轮工具调用/SSE/resume、临时配置清理、凭据加密边界及 logout/remove。 |
| `claude-mcp-page-performance-real.spec.ts` | 可选真实账户 MCP 列表性能流程 | 只读采样可见 Resources 首次进入、刷新和重复进入的 P50/P95，断言列表关键路径零 discovery、零 MCP 写请求并服从独立首屏预算。 |
| `claude-mcp-cancel-real.spec.ts` | 可选真实账户 MCP cancel 流程 | 可见取消等待授权的 OAuth operation，并在 managed-MCP Chat 运行中点击停止生成；验证零凭据、Runtime idle、无 pending confirmation、临时 Server 清理及 Thread 保留。 |
| `claude-mcp-chat-real.spec.ts` | 可选真实账户 MCP Chat 流程 | 验证已连接服务进入可见 Chat、精确线程工具确认、持久化结果以及刷新后的同线程 resume。 |
| `claude-runtime-failure-real.spec.ts` | 可选真实 Runtime 故障流程 | 对唯一测试自有候选 Runtime 子进程执行 fail-closed 异常退出，验证可见安全错误和同 Thread 恢复。 |
| `claude-runtime-request-fields-real.spec.ts` | 可选真实 Runtime 请求合同流程 | 通过可见 Chat 首轮与同 Thread resume，读取 content-free Gateway 投影证明 `max_tokens`、条件 effort、`stream:true` 和 Authorization 脱敏。 |
| `admin-mcp-gateway-visibility-real.spec.ts` | 可选真实 Admin 可见性流程 | 通过正常 Admin 登录只读查询本轮 Gateway 请求详情与 subscription Token 流水，并显式登出。 |
| `claude-agent-image-buffer-real.spec.ts` | opt-in image message journey | Verify a real image Read survives the bounded SDK message buffer through the public Chat path. |
| `chat-dream-agent-refactor.spec.ts` | Chat/Dream Agent provider-free journey | Prove Dream excludes the deferred marketplace, historical Chat keeps immutable Deck provenance while switching its next-turn Agent from the top badge, fresh Chat keeps selection, plus borderless layout, active reveal, two-state re-entry, adaptive Chat list, canonical navigation, and launch dispatch at wide/narrow/low-height viewports. |
| `dream-*.spec.ts` | Dream journeys | Cover provider-free and explicitly named PostgreSQL/real-model Dream flows. |
| `story-workspace-*.spec.ts` | Story Workspace journeys | Cover subscription, execution, artifact, title, skill, and workbench behavior. |
| `story-workspace-real-asset-rendering.spec.ts` | opt-in real asset rendering journey | Use the named actor and existing Run to prove YAML frontmatter, Markdown prose, and storyboard note IDs remain readable at wide and narrow viewports without data mutation. |
| `model-settings-gateway*.spec.ts` | model settings journeys | Cover mocked and real Gateway model selection behavior. |
| `registration-defaults-postgres-real.spec.ts` | isolated full-stack registration journey | Cover canonical registration, Free defaults bootstrapped from a priced model without prior entitlement, Admin model selection, and first Agent call through the dedicated harness. |
| `fixtures/` | browser harness fixtures | Provide reusable Story Workspace and model-settings harnesses. |
| `helpers/` | E2E helper scripts | Read or prepare bounded test facts for explicitly scoped workflows. |

## Sync

- 2026-09-01: Added a technical isolated Writing suggestion browser journey with real frontend state/controller/UI seams, deterministic provider-free SSE, same-Thread evidence, and light/dark desktop/mobile screenshots.
- 2026-09-01: the Writing browser journey proves a newly appended suggestion hides Refresh on every historical Cell while the latest retains the sole localized action.
- 2026-08-31: Execution provider-free journey verifies the Dream run-title opens the independent static creation-guide route while the Outline trigger stays in place; both reuse three read-only workflow stages and generated illustrations, return to their real source, and remain overflow-safe without workflow writes.
- 2026-08-31: the authenticated-entry journey rejects the historical `/ink-and-memory/` surface and exercises Writing's related Thread Chat open, drag-resize, close, and visual layout behavior.
- 2026-08-29: the provider-free Notion Settings journey now covers the implemented seven-section IA and four child views, proves overview/Skill reads do not trigger provider discovery, validates safe information links, and checks wide/narrow single-scroll layout.
- 2026-08-29: the Notion Settings journey locks the skeleton-proportioned spacing and verifies overview section subtitles, Skill summaries, resource counts, and recent-sync copy stay absent.
- 2026-08-30: the Notion Settings journey requires capability schema v4 installation metadata, both installed Skill rows, connected `notion-cli` availability, and its real `ntn api` body.
- 2026-08-30: expanded opt-in real Notion CLI acceptance to three persisted turns: safe new-turn and resume Bash receipts plus an ordinary no-tool Chat regression; the existing connector, remote content, subscription, and generated review thread are preserved.
- 2026-08-29: added a provider-free focused Notion Settings harness and Chromium journey; it locks the disabled Feishu/local CLI discovery placeholders, touches no real account/provider data, and audits application console, page, and request failures.
- 2026-08-28：Runtime `0.1.3` 真实验收要求 Gateway `max_tokens` 精确等于认证 Admin 模型能力，同时只读取模型、effort、stream 和脱敏 Header，不输出正文或凭证。
- 2026-08-25：Provider-free MCP Resources 验证详情页自动以 `force: false` 加载 inventory，且页面不存在刷新或重试 inventory 控件。
- 2026-08-25：真实 OAuth 验收删除 redirect 文件、输入框和手工提交 fallback；Provider consent 后必须由同源 `/oauth/callback` 自动提交 operation 并关闭 popup，测试不读取或回填 code/state。
- 2026-08-25：provider-free 浏览器回归验证 `/oauth/callback` 只由 SPA 呈现，以非敏感 operation ID 自动提交，正文/localStorage 不含 code/state，且 query 不会进入后端 access log。
- 2026-08-25：新增真实 MCP cancel 验收；OAuth 仅到 waiting-for-user 后可见取消且不创建凭据，Agent turn 仅在后端确认 running 后可见停止并要求明确 idle 回执。
- 2026-08-25：新增真实 MCP Resources 数据库列表性能验收；首次进入、刷新与重复进入分别输出 P50/P95，并确保采样不触发远端 discovery 或 MCP 配置写入。
- 2026-08-25：真实 MCP Resources 验收共用同一条可见业务旅程覆盖 Streamable HTTP、legacy SSE 与 server-owned stdio，并精确断言 tools/resources/prompts 数量；stdio 不接受浏览器 URL、命令或凭据。远端 Server 尚在 pending 时只允许参数恰为当前单 Server 的内建只读 `WaitForMcpServers`，其余未知确认继续 fail closed。
- 2026-08-25：真实 MCP Resources 验收改查 `dream_mcp_*` 事实与 turn 后零临时配置文件，不再读取 Claude CLI 配置、Keychain 或 Runtime OAuth 文件；inventory 只在详情页自动触发。
- 2026-08-25：真实 MCP Chat 对话改用页面可理解的自然语言业务请求；精确只读 tool 名只用于断言和确认 allowlist，不再作为给模型的内部执行指令。
- 2026-08-25：真实 MCP Resources 验收只确认 `INK_REAL_CLAUDE_MCP_SAFE_TOOLS` 显式集合中的只读工具，并在成功或失败清理时通过公开会话端点关闭本轮 Thread Runtime；未知工具继续 fail closed。
- 2026-08-25：真实 MCP resume 验收在 Runtime 转为 idle 后继续等待持久化 tool/result 与 assistant parts 可见，避免把异步落库窗口误报为未调用工具。
- 2026-08-25：真实 OAuth operation 创建先断言 HTTP 202 并保留后端安全错误 DTO，避免把配置门禁误报为前端 TypeError。
- 2026-08-25：新增 MCP 发布验收的真实 Admin 可见性流程，精确核对 Gateway 请求终态与 reserve/capture/release Token 流水。
- 2026-08-25：新增候选 Runtime 真实异常退出验收；仅允许精确 backend 父链下、本轮唯一新增的 Runtime leaf，并验证同 Thread 恢复。
- 2026-08-25：真实 Dream 模型验收从当前生产 Chat composer 选择 Dream Deck 并发起 Run，不再依赖只读 Dream 首页上的旧启动表单。
- 2026-08-25：MCP 浏览器验收采用 Runtime 返回的 `anonymous`、`required`、`authenticated` 认证状态；真实用例可验证匿名服务不会自动 OAuth，并保留受保护服务的显式认证、取消与退出路径。
- 2026-08-25：MCP 新增/编辑页面不再选择认证方式；技术与真实用例进入详情后自动 discovery，由后端判定匿名或 OAuth-required 后再展示对应动作。
- 2026-08-25：真实 MCP Chat 验收接受显式只读工具输入和 `authenticated` 状态标签，并在本机真实业务模式下从 Admin 指定的 PostgreSQL 环境文件解析现有账户。
- 2026-08-24：真实 MCP OAuth 验收不再以直连 API 和工具名片段代替业务结果；现在从可见 Chat 发起 turn，只确认指定 MCP 工具，并验证持久化 `output-available` 后再退出和移除服务。
- 2026-08-24：真实 MCP OAuth 验收扩展为刷新后继续第二轮，两轮都必须复用同一 Thread、调用同一只读 MCP 工具并持久化 `output-available`，随后才 Logout/Remove。
- 2026-08-24：真实 MCP OAuth 验收接受 Logout 后明确的“已退出”或重新探测后的“需要认证”展示态，并继续用凭据投影删除断言验证真实清除，再执行 Remove。
- 2026-08-24：真实 MCP Chat 两轮流程将确认请求绑定到精确 Thread，并要求 canonical confirmation API 返回成功后才接受 UI 结算。
- 2026-08-24: provider-free MCP Resources and Dream/Chat convergence harnesses now serve the production shell's authenticated session autosave and read-only Dream Run collection while preserving strict unexpected-request failure assertions.
- 2026-08-24: real-model Dream launch waits for the Story Index semantic `indexed` state before choosing an enabled visible recovery action, avoiding the transient visible-disabled retry render without sleeps or forced clicks.
- 2026-08-22: restored provider-free and explicitly opt-in Claude MCP browser journeys; technical fixtures contain no real OAuth secrets.

- 2026-08-19: `claude-plugin-settings.spec.ts` covers Comfy global catalog selection, immutable revision copy, captured entry-ID install, operation result, capability failure/reload, focus return, Escape, and 390px placement without calling the real backend.
- 2026-08-16: Deck browser journeys remove page-level use/create tab steps, assert a toolbar-first management route, and exercise Deck/Agent selection from the actual Chat consumer.
- 2026-08-17: The provider-free Deck journey verifies published-clean user shortcuts, system markers, exclusion of unpublished/draft/disabled user Decks from home, and their continued availability in Work.
- 2026-08-17: Added opt-in read-only real-content proof for the named actor's user published-clean Deck home, default-visible system Decks, and complete Settings / Work inventory.
- 2026-08-17: Deck journeys verify locale-specific Settings/Work navigation and headings, including live en→zh→en switching and the real Chinese route without a bilingual title.
- 2026-08-17: Deck journeys verify persistent Available/System launcher groups, per-group empty states, and static registered system defaults with no open/edit/fork/publish UI.
- 2026-08-17: Deck journeys verify user and System Deck cards open the shared preview page; System Decks remain read-only there while real-account checks stay non-mutating.
- 2026-08-17: Deck preview example journeys prove the matching Deck/Agent and visible text reach Chat while Thread/message mutations remain zero until the user sends.
- 2026-08-17: Typed Deck preview journeys now prove Chat examples stay editable/unsent while DreamAgent examples call Dream start and enter the dedicated run workbench.
- 2026-08-17: Historical Chat browser coverage switches between enabled Agents in one bound Deck while the same Thread and plugin receipt remain visible.
- 2026-08-17: Named-account read-only QA verifies real multi-Agent Deck content and zero Chat writes; it performs the same-Thread selection only when the account already has an eligible bound Thread, otherwise it verifies the no-Thread disabled boundary without seeding data.
- 2026-08-16: The provider-free Deck journey locks the PDF layout, exact binding v/revision facts, folded history, v1.0.1→v1.1.0 CAS, restored maintenance, Chat handoff, wide/narrow screenshots, and zero Workflow/market requests.
- 2026-08-16: Dream removes the community Deck installation surface and its opt-in real-market spec; marketplace requirements remain deferred under `docs/design/deck-register/`.
- 2026-08-16: Real historical-thread model QA now selects the latest repeated query, so preserved prior acceptance messages do not create a strict-selector false failure.
- 2026-08-15: The authenticated-entry journey verifies More is collapsed by default, expands to Writing/Timeline/Reflections, and keeps Chat as the root login destination.
- 2026-08-15: Wide/narrow provider-free Chat coverage proves historical conversations hide the immutable composer selector, keep the Deck name visible after plugin packing, and let fresh conversations retain Deck → Agent selection.
- 2026-08-15: Deck journeys now assert the default read-only use mode, Deck/Agent handoff to Chat, explicit creator-mode writes, and refreshed mode reset.
- 2026-08-14: Registration coverage now proves the Free seed can bootstrap the selected priced model's first Entitlement before browser signup and the first settled Agent call.
- 2026-08-14: Added the folder contract and registered Chat-first/Deck-default browser coverage with strict production-shaped mocks and no real business-data writes.
- 2026-08-14: The authenticated-entry journey now locks the visible sidebar order to Chat, Dream, Decks.
- 2026-08-14: Default-Deck coverage now starts from an empty legacy ref set; an opt-in real-account spec corroborates the visible checkbox with the authenticated persisted refs API.
- 2026-08-14: Added provider-free wide/narrow Chat/Dream Agent refactor coverage and screenshots; it does not claim a real model or real-account run.
- 2026-08-14: Added low-height page-scroll recovery and whole-card Active Dream workbench navigation to the provider-free journey.
- 2026-08-14: Added opt-in named-account Deck sharing acceptance for actor-owned publications and server-enforced publish/collection rules.
- 2026-08-14: Dream home coverage now asserts that the first section is actor-scoped in-progress Run/Deck context and navigates through the same canonical `run.href` as Chat Active Dreams.
- 2026-08-14: Browser coverage now asserts initial/in-progress-only labels plus a real horizontal overflow axis with no nested vertical scroller.
- 2026-08-14: Dream home coverage now locks the borderless active index, three-item default preview, explicit reveal/collapse state, and wide/narrow visual evidence.
- 2026-08-14: Community Deck coverage now requires the active system default to appear with a visible `System default Deck` source label.
- 2026-08-14: Community Deck coverage also proves the named-account-compatible fallback to an actor-owned `default_initialized` Deck when the shared system row is absent.
- 2026-08-14: Added read-only real-run asset rendering coverage for nested character metadata, Markdown body typography, true storyboard IDs, dark wide/narrow overflow, and canonical/stage immutability.
- 2026-08-14: Execution coverage starts on the uncollapsed Dream draft, switches to the exclusive Episode coordination surface from the Agent header beside Chat, and proves narrow selection closes the modal before revealing the chosen workbench.
- 2026-08-31: Execution provider-free and opt-in real browser coverage opens the canonical file reader from the matching draft EP; sync contains no reader and its read actions return to the EP-owned tabs.
- 2026-08-31: Execution provider-free coverage requires the draft EP description to show projected shot count/duration only, hides flattened storyboard payload text, and removes the duplicate focus overview.
- 2026-08-14: Added read-only real-run acceptance for the default Draft surface, adjacent Draft/Sync/Chat controls, refresh reset, narrow modal handoff, and workspace/stage immutability.
